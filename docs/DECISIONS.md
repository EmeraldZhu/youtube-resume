# Decision Log — YouTube Resume

Append-only. Never delete a row; change its Status instead. IDs are sequential and never reused.

**Status:** `OPEN` (unresolved) · `APPROVED` (decided, not yet built) · `DONE` (built and confirmed) ·
`REJECTED` (decided against) · `SUPERSEDED` (replaced by a later ID — name it in Notes)

**Owner:** who resolves it. `Human` means it waits for the owner. `Claude` means Claude Code closes it
autonomously and logs the outcome.

**Read this file at the start of every phase.** Any row whose **Implement In** matches the current
phase is in scope for that phase, whether or not the phase prompt mentions it. Say so up front.

**Add rows without being asked.** Every Tier 2 and Tier 3 decision gets one, at the end of the phase
that made it. A decision that exists only in a chat report is lost when the session ends.

---

## Product & scope

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-001 | Release is **v2.0.0**, not v5.0.0 | APPROVED | Human | All | v1.0.0 is the live Web Store version. |
| D-002 | Settings page promoted into scope, reversing v1.0 non-goal NG4 | APPROVED | Human | Phase 6 | Deliberate promotion. NG4 removed from PRD §3.2. |
| D-003 | Saved videos panel promoted from PRD §13 roadmap into v2.0 scope | DONE | Human | Phase 8 | Was "Resume history page", Medium priority. |
| D-004 | Thumbnails permitted from `i.ytimg.com`, breaking the absolute zero-network claim | DONE | Human | Phase 8 | A list of opaque video IDs isn't usable. Requires D-020 before publishing. |
| D-005 | Thumbnails have an off switch, default on, restoring fully-offline operation | APPROVED | Human | Phase 6 | Mitigates D-004. When off, `src` must not be set at all. |
| D-006 | No new permissions: `<img>` for thumbnails, `<a target="_blank">` for opening videos | DONE | Human | Phase 8 | Avoids the `tabs` permission entirely. |
| D-007 | Six settings only; the 400ms delay, 5s interval and 200-entry cap stay hard-coded | APPROVED | Human | Phase 6 | Exposing the 400ms delay invites users to break their own resume. |
| D-008 | Settings render as a second view **inside the popup** — not an options page, not a new tab | APPROVED | Human | Phase 6 | Owner explicitly rejected the new-tab pattern. |
| D-009 | Settings use segmented buttons and toggles, never free numeric input | APPROVED | Human | Phase 6 | Eliminates validation and invalid states on a 360px surface. |
| D-010 | Popup widens 280px → 360px; UX Spec §6.2 "must not feel like an app" formally relaxed | DONE | Human | Phase 8 | A thumbnail list is unusable at 280px. |
| D-011 | Status row (`✓ Active on YouTube`, CP-12/CP-13) removed | DONE | Human | Phase 8 | A populated list is self-evident proof; empty state covers the zero-data case. |

## Storage & data

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-012 | Schema advances to v2, adding optional `title` | DONE | Human | Phase 4 | Purely additive, so migration is non-destructive by construction. |
| D-013 | Schema version lives in root key `youtubeResumeSchema`, never inside `youtubeResume` | DONE | Human | Phase 4 | `youtubeResume` keys are counted for eviction; a stray key corrupts it. |
| D-014 | Settings live in root key `youtubeResumeSettings` | DONE | Human | Phase 4 | Clearing progress must not reset settings, and eviction must never eat one. |
| D-015 | Title scraped from `document.title` with the ` - YouTube` suffix stripped, DOM selector as fallback | DONE | Claude | Phase 4 | `document.title` is far more stable across YouTube redesigns than metadata selectors. |
| D-016 | Stored titles capped at 200 characters | DONE | Claude | Phase 4 | Tier 2 value pick. |
| D-044 | `popup.js` stopped calling `chrome.storage.local` directly (pre-existing v1.0 violation of the "storageManager is the sole owner" constraint); popup now loads `storage/storageManager.js` and uses new `getAllProgress()`/`clearAllProgress()` methods | DONE | Claude | Phase 4 | Found while doing T4.9 (grep for `chrome.storage`). CLAUDE.md hard constraint — route around, don't ask. `clearAllProgress()` removes only `youtubeResume`, leaving settings/schema untouched (PRD §7.4). |
| D-045 | `saveProgress`'s new optional `title` param preserves an existing stored title when omitted/falsy, instead of erasing it | DONE | Claude | Phase 4 | Roadmap 4.5 says a missing title capture "must never block the save" but doesn't say whether it should clear a previously-good title; erasing on a transient capture failure (e.g. `document.title` momentarily blank during a redesign/SPA transition) would be a silent regression for panel display (Phase 8). Tier 2 value pick. |

## Reliability (Phases 1–3)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-017 | Confirm which of hypotheses H1–H8 actually cause unreliable resume before fixing anything | DONE | Claude | Phase 1 | `docs/PHASE1_FINDINGS.md`. H2/H4/H6/H8 confirmed; H5 not reproduced (TDD doc issue only); H1 confirmed via a different trigger than described (see D-036); H3/H7 confirmed by code but not reproducible live this session; H1's ad case and H3's throttled case untested (S4, see Notes). |
| D-018 | If Phase 1 finds failure modes outside H1–H8, Phase 2 scope expands to cover them | DONE | Claude | Phase 2 | Expanded per D-036/D-037/D-038 below; all three built this phase. |
| D-036 | Phase 2 must fix navigationManager/bootstrap teardown gap: leaving a watch page for a non-watch page never emits, so progressTracker/uiInjector/playerObserver teardown never runs | DONE | Claude | Phase 2 | Phase 1 Finding A. `checkAndEmit()` now emits on any change including transitions to/from `null`, not just truthy new ids — this single change also fixes H8 (same-video re-entry, D-023 row below/task 2.5), since leaving resets `currentVideoId` to `null`. |
| D-037 | Phase 2's ad/guard redesign (D-019/D-021) must also account for YouTube's own native resume moving `currentTime` before the 400ms delay elapses, independent of ads | DONE | Claude | Phase 2 | Phase 1 Finding B. New guard compares against a pre-delay baseline with 10s drift tolerance rather than an absolute `currentTime > 5`, so a native resume landing near the saved position no longer trips it. |
| D-038 | `waitForMetadata`'s 5s timeout is reachable under ordinary (non-throttled) network conditions and silently skips resume; Phase 2 should reassess the timeout or add a retry | DONE | Claude | Phase 2 | Phase 1 Finding C. Implemented as one retry (second 5s wait) before giving up, ~10s total — Tier 2 value pick. |
| D-019 | Resume is ad-gated: defer until `.ad-showing` and `.ad-interrupting` both clear | DONE | Human | Phase 2 | PRD §5.7 required this in v1.0; the v1.0 TDD omitted it, so it was never built. Implemented as a bounded loop: an ad reappearing mid-delay re-defers and re-baselines rather than aborting outright (see D-040). |
| D-020 | Ad wait has a 60-second hard ceiling, then abandons cleanly | DONE | Claude | Phase 2 | Tier 2 value pick. Prevents an unbounded wait; polled every 250ms via recursive `setTimeout` (not `setInterval`, to respect the one-interval constraint). |
| D-021 | Replace the `currentTime > 5` abort guard with a pre-delay comparison, 10s drift tolerance | DONE | Claude | Phase 2 | The old guard read *ad* position, silently cancelling resume on any ad over 5s. |
| D-022 | Seek is verified: re-read after 250ms, re-assign if off by >3s, max 3 attempts | DONE | Claude | Phase 2 | Tier 2 value picks. Never an unbounded retry loop. UI (restart button/toast) still shown even if verification never converges — best-effort seek already occurred. |
| D-023 | `waitForVideo()` waits for `#movie_player` instead of rejecting when it's absent | DONE | Human | Phase 2 | v1.0 guaranteed a missed resume on slow cold loads. Observes `document.body` (covers container-missing and video-missing in one observer). |
| D-040 | Ad reappearing during the 400ms resume delay re-defers to the ad wait and re-baselines the drift guard, instead of abandoning the resume outright; bounded to 3 rounds | DONE | Claude | Phase 2 | PRD §5.7 says defer until ads clear, not abandon on the first mid-delay ad — matches mid-roll-heavy long videos better than a single abort. Round cap is a Tier 2 value pick to prevent an unbounded loop. |
| D-041 | Restart button and toast now gated on `seekWithVerification` actually succeeding; a failed verification shows neither and only logs a warning | DONE | Claude | Phase 2 | T2.1 verification failure: toast claimed "resumed from 19:58" while playback actually continued from 19:40 — first-cut code showed the UI unconditionally after the seek attempt regardless of verification result. PRD §5.6 already required this ("Restart button appears only when a resume seek was successfully applied and verified") — the bug was not implementing that gate, not a missing spec. Root cause of the drift itself is presumed to be YouTube's own native resume cue re-asserting a stale remembered position during the verify window (Finding B); no code change beyond the UI gate was made for that, since D-021/D-022's bounded guard+retry design is already the agreed mitigation. Owner re-ran T2.1 (3 cold loads, including incognito) after the fix: all 3 resumed within ±3s. |
| D-024 | Delta guard applies to the interval trigger only; all event triggers save unconditionally | DONE | Human | Phase 3 | Resolves a contradiction inside v1.0 TDD §4.5. `attemptSave(bypassDelta, trigger)` takes an explicit flag; only the interval call passes `false`. |
| D-025 | `beforeunload` retired in favour of `pagehide`; both documented as best-effort | DONE | Human | Phase 3 | v1.0 claimed a synchronous storage save, which that API cannot do. TDD §4.5/§7.2 updated to state best-effort explicitly. |
| D-042 | `ended` handler added; invalid-position guard (`NaN`/negative/`>duration`) added to `attemptSave` | DONE | Claude | Phase 3 | Roadmap 3.2/3.5. Closes Phase 1 Finding H6 (no `ended` handler). Both are Tier 2 — filled a task-list detail (guard specifics weren't spelled out) rather than a new decision axis. |
| D-043 | `attemptSave`'s invalid-position guard also rejects `NaN` `duration` (not just `NaN`/negative/`>duration` `current`); `tryResume` gains a duration-independent `meetsMinimumWatched()` short-circuit before the metadata wait | DONE | Claude | Phase 3 | Found via owner's live extension-errors report: `current > duration` is always `false` in JS when `duration` is `NaN`, so the D-042 guard didn't actually block a `NaN`-duration save (pre-metadata write); separately, `tryResume` was paying for the full ~10s metadata wait (D-038) even when `saved.time` could never clear the 30s minimum regardless of duration, observed live on a 19s video. |

## UI (Phase 5)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-026 | Measure YouTube's live computed styles and record them in `docs/YT_DOM_AUDIT.md` before restyling | DONE | Claude | Phase 5 | Values are measured, never remembered. Blocks D-027 and D-028. |
| D-027 | Reverse the v1.0 "no background or border on the Restart button" anti-pattern | DONE | Human | Phase 5 | YouTube moved to rounded-pill controls; the old rule now *causes* the mismatch. |
| D-028 | Fix the toast/progress-bar collision by deriving the offset from measured control-bar height | DONE | Human | Phase 5 | Confirmed in the owner's screenshot: the red line runs through the toast. `bottom: 48px` is stale. |
| D-029 | Toast promoted from optional to required, and made user-disableable | DONE | Human | Phase 5 | It already ships; specifying it properly is overdue. Disable switch itself is `showToast`, wired in Phase 7 — this phase only fixes its visuals and confirms it's unconditional pending that setting. |
| D-046 | Measurement (`docs/YT_DOM_AUDIT.md`) found no native inline text-pill button in the control bar — icon buttons are flat with opacity-only hover. Restart button pill fill/radius instead derived from the measured `.ytp-menuitem` hover intensity (`rgba(255,255,255,0.1)` rest / `0.2` hover) and the 40px control-row height (`20px` radius); toast background/radius taken directly from the measured `.ytp-settings-menu` panel (`rgba(0,0,0,0.6)`, `12px`) | DONE | Claude | Phase 5 | Tier 2 value pick, filling the gap between D-027's approved reversal and what measurement actually showed. Toast's `bottom` offset is computed at runtime from `.ytp-chrome-bottom`'s measured height (`59px`, identical in default/theater) + 12px clearance, not hard-coded, per D-028. |
| D-047 | Restart button gains `alignSelf: 'center'` | DONE | Claude | Phase 5 | Owner's T5.1 verification found the button sitting flush against the progress bar instead of centered like native controls. Root cause: `.ytp-left-controls` is `display: flex; align-items: normal` (stretch); a fixed-height child (our 40px button) without `align-self: center` collapses to flex-start instead of centering like the native 40px icon buttons, which are centered by the same mechanism. Verified via measured `getBoundingClientRect()`: button's top/bottom now match `.ytp-play-button`'s exactly. |
| D-048 | `storageManager.js` gains an `assertStorageAvailable()` guard at the top of every public function, throwing a descriptive error when `chrome.storage.local` is unavailable | DONE | Claude | Phase 5 | Owner's T5-verification first-load report: `Cannot read properties of undefined (reading 'local')` on the very first resume attempt. Root cause is almost certainly a dev-only artifact — a content-script instance left running in an already-open tab from before the extension was last reloaded via Load Unpacked, whose `chrome.storage` binding is invalidated (a real store install never hits this, since new tabs/reloads always get the current content script). Behavior is unchanged (resume already failed safely, caught by the existing `.catch()`); this only makes the console warning diagnosable instead of a bare `TypeError`. |

## Docs & release

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-030 | TDD stays at v1.0.0 and is updated section-by-section per phase, reaching 2.0.0 in Phase 9 | APPROVED | Human | All phases | Precedence puts TDD above PRD/UX Spec, so a stale TDD is a live risk. Every phase lists its sections. |
| D-031 | Dev Checklist §6.2 arithmetic error resolved: `shouldResume(100, 200)` is `true`, resumes to 98 | APPROVED | Human | Phase 9 | 100 < 190. The v1.0 test case was simply wrong and self-noted as such. |
| D-032 | Privacy policy must disclose the `i.ytimg.com` thumbnail request | DONE | Human | Pre-release | S3 — published under the owner's name. Draft written at `docs/PRIVACY_POLICY_DRAFT.md` (Phase 9); reviewed and approved by the owner. |
| D-033 | Store listing must stop claiming zero network requests | DONE | Human | Pre-release | S3. Draft written at `docs/STORE_LISTING_DRAFT.md` (Phase 9); reviewed and approved by the owner. |
| D-034 | Whether to ship v2.0.0 as one release or split reliability (1–3) into a v1.1 patch first | OPEN | Human | Pre-release | Reliability alone would help live users sooner. Worth revisiting after Phase 3. |

## Runtime wiring (Phase 7)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-049 | `storageManager` gains a synchronous `getDefaultSettings()` (no storage access); `bootstrap.js` calls it only when `getSettings()` itself rejects, wrapping the call in try/catch | DONE | Claude | Phase 7 | Roadmap 7.7 requires a settings failure to never block resume. `getSettings()` already self-heals a corrupt *value* (merges over defaults), but a rejected promise (e.g. `chrome.storage` unavailable) needed a fallback that doesn't itself touch storage — duplicating the defaults object outside `storageManager` would drift, so it's exposed instead. Tier 2 value pick. |
| D-050 | `progressTracker`'s `minWatchSeconds` gate reuses `timeUtils.meetsMinimumWatched(current, minWatchSeconds)` against the live playback position, mirroring `resumeManager`'s use of the same function against `saved.time` | DONE | Claude | Phase 7 | Roadmap 7.5 specifies the outcome ("no storage entry below this threshold") but not the mechanism. Reusing the existing position-based predicate avoids a second threshold concept; applies to every save trigger (interval and event), not just the interval, since none of them should persist an entry below the floor. Tier 2 value pick. |

## Saved videos panel (Phase 8)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-053 | `.list-region` gets a static `max-height: 508px` (560px popup max minus the measured ~52px fixed header) with `overflow-y: auto`, instead of relying on flexbox `flex: 1` to derive the remaining height | DONE | Claude | Phase 8 | Roadmap 8.1 specifies the outcome (560px max, header fixed, list scrolls) but not the mechanism. A `flex:1; min-height:0` child inside a `max-height`-only, `height:auto` flex column does not get clamped — the flex container's own resolved height is computed from content *before* `max-height` is applied as a ceiling, so nothing forces the list to shrink; discovered live via `chrome-devtools-mcp` when a 200-row seed rendered as one long unclamped page instead of a 560px popup. A direct `max-height` on the scrolling element itself sidesteps the flex sizing question entirely and still shrinks-to-content for short lists (empty state, a handful of entries), matching "maximum height" rather than "fixed height". |

## Saved videos panel polish (post-Phase-8, owner-requested)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-054 | Video duration and a watched-progress line render directly on the thumbnail (YouTube-style bottom-right badge + bottom-edge fill), built as plain text/CSS elements rather than baked into an image | DONE | Human | Phase 8 polish | Owner asked for a duration overlay and recommended a placement for time-watched; decided to keep the precise `{position}/{duration} · {percent}%` text below the thumbnail (existing CP-34/35) and use the thumbnail overlay only for the at-a-glance YouTube-style badge + progress line, avoiding two redundant progress indicators stacked in one row. Because these are text/CSS, not an `<img>`, they still render when `loadThumbnails` is off — D-005's zero-network guarantee is about image requests, not DOM content, so this doesn't reopen it. |
| D-055 | Thumbnail size increased 120×68 → 144×81 (same 16:9 ratio) | DONE | Human | Phase 8 polish | Owner asked to increase thumbnail size to give the new overlays (D-054) and channel name (D-056) more room. Text column narrows from ~176px to ~172px at the fixed 360px popup width (D-010) — still fits a 2-line title, one channel line, and the meta line without crowding. |
| D-056 | Schema gains an optional `channel` field, captured via new `youtubeUtils.getChannelName()` (DOM selector only — unlike title, there's no `document.title` equivalent for channel name) and persisted through `saveProgress`'s existing preserve-if-omitted pattern (D-016) | DONE | Human | Phase 8 polish | Owner asked for the channel name to display next to the thumbnail, matching YouTube's own sidebar. Selector scoped to `ytd-watch-metadata ytd-channel-name a` / `ytd-video-owner-renderer #channel-name a` (verified live against a real watch page) rather than a bare `#channel-name`, which also matches inside the comments section. Purely additive like `title` in schema v2 (D-012) — no schema version bump needed. Rows with no captured channel (pre-existing entries, or a same-session DOM-selector miss) simply omit the channel line rather than showing a placeholder. |
| D-057 | `youtubeUtils.getTitle()` also strips a leading `(3) `-style unread-notification-count prefix from `document.title`, in addition to the existing trailing `" - YouTube"` suffix | DONE | Human | Phase 8 polish | Owner flagged that YouTube prepends an unread-count badge to the tab title. Regex (`/^\(\d+\)\s*/`) matches digit-only parenthetical prefixes only, so a legitimate title parenthetical like `"(Official Video)"` is left untouched — verified against both cases. |
| D-058 | Icon-only Ko-fi link added to the saved-videos header, centered via a 3-column `header-row` grid (title / icon / count+gear), as an inline SVG rather than the ❤️ emoji already used once in Settings | DONE | Human | Phase 8 polish | Owner asked for a coffee icon linking to ko-fi.com in the header, icon only, no text. UX Spec §9.2 reserves the ❤️ emoji as the *single* intentional emoji exception, used only in Settings — reusing it (or adding a second emoji) here would break that rule, so a hand-drawn inline `<svg>` (no external asset, no network request, no `innerHTML`) was used instead. New aria-label copy **CP-61** `Support on Ko-fi` — icon-only, so the accessible name is the only copy this control has. Verified live: opens `https://ko-fi.com/emeraldzhu` in a new tab. Icon swapped once (owner-supplied markup, a mug-with-steam-lines glyph from a reference extension) and enlarged 16px → 20px after the first pass read as too small in the header. |

## Phase 9 — Integration, Regression & Store Resubmission

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-059 | Fixed rather than just documented: `progressTracker` no longer owns its own `setInterval`. It exposes `tick()`, called from `navigationManager`'s existing permanent 1000ms poll (wired through `bootstrap.js`), and fires a save every 5th tick to preserve the exact 5000ms cadence. `navigationManager.start()` gained an optional second `onTick` param. Restores CLAUDE.md's "exactly one `setInterval`" constraint literally, not just in spirit. | DONE | Claude | Phase 9 | Found by live-testing per Roadmap T9.4: two intervals (`navigationManager`'s 1s URL-poll fallback, pre-existing since v1.0, and `progressTracker`'s 5s save interval) ran concurrently on any watch page, contradicting the constraint and TDD §11.3/§9's "1 max" claim. Initially logged as accept-and-document-only, given the risk of touching hardened, ad-gating-tested tracking code this late. Revisited same session at owner's request after sizing the fix as small and low-risk: it changes only *how* the 5s cadence is clocked (tick-counting off an existing beat vs. an independent timer), not any save-trigger guard, ad-gating, delta logic, or timing value — `attemptSave`'s call sites, arguments, and every guard in it (D-024, D-042, D-043, D-050) are untouched. `stop()`/`start()` reset `ticksSinceSave` to 0 exactly where the old code recreated the interval, so first-save-after-video-change timing is unchanged. **Live-verified same session** via `chrome-devtools-mcp` (D-051/D-052 technique) plus a temporary `DEBUG=true` flip (reverted before finishing, never committed true): resume seeked correctly (200s saved → 198s landed, 0 drift), and the interval-trigger save fired ~30 times over a 131s window (≈1 per 4.4s, matching the intended 5s cadence — not the underlying 1s poll rate, and not stalled). A separate, unrelated video-element reset mid-session (currentTime/duration went to 0/NaN, likely a CDP-driven-browser autoplay/stall artifact) was correctly caught and skipped by the existing invalid-position guard (D-042/D-043), which is the guard working as intended, not a new bug. |
| D-060 | T9.3 (30-min soak) and T9.7 (20-rep SPA-nav resume) scoped down this session to what the sandbox's `chrome-devtools-mcp` Chrome instance can reliably run, instead of blocking Phase 9 on them | DONE | Claude | Phase 9 | This session's browser hit real `googlevideo.com` CDN `403`s and an unexplained mid-session `currentTime`/`duration` reset to `0`/`NaN` on real long-video playback — an environment/network limitation (same class of issue D-051 hit with `claude-in-chrome`'s video buffering), not an extension defect; console showed no `[YTResume]` errors correlated with either. Substituted: (1) the resume mechanism itself, re-verified via the D-052 reload+precondition technique — same code path SPA navigation uses once `bootstrap.onVideoChange` fires; (2) the navigation-detection/teardown-reinit layer, stress-tested via 25 synthetic `yt-navigate-finish` dispatches (D-062) instead of 25 real video loads, avoiding multiplying the CDN-403 risk. Full real-video 30-min soak and a real 20-rep SPA-nav sample remain for the owner to run in a normal browsing session, alongside T9.1 — noted in the release checklist, not silently dropped. |
| D-061 | Restart-button click-clears-storage-and-seeks-to-0 (previously inconclusive per prior session) re-verified this session | DONE | Claude | Phase 9 | Via D-052's reload+precondition-write technique: wrote a `youtubeResume` entry (`time:200, duration:596`) directly from the popup context, reloaded the video tab, resume landed at 198s with button+toast shown, then a scripted click on `#yt-resume-restart-btn` seeked the video to ~0.01s and removed the button; `chrome.storage.local.get('youtubeResume')` read back `{}` immediately after. All three pass conditions (seek to 0, storage cleared, button gone) confirmed in one run. |
| D-062 | T9.4 (25 consecutive SPA navigations, single-interval/no-leak check) verified via synthetic navigation instead of 25 real video loads | DONE | Claude | Phase 9 | `history.pushState` to 25 distinct fake video IDs + `document.dispatchEvent(new Event('yt-navigate-finish'))` in a tight loop, sampling for leaked `#yt-resume-restart-btn`/`#yt-resume-toast` elements after each — zero leaks across all 25 teardown/re-init cycles, matching D-059's single-interval design (no per-video interval exists to leak). Side effect noted, not a bug: a bare `Event` (vs. YouTube's own `CustomEvent` carrying `detail.endpoint`) makes YouTube's *own* router code throw `Cannot read properties of undefined (reading 'endpoint')` — confirmed unrelated to `[YTResume]` code by symptom (the string `endpoint` doesn't appear anywhere in this extension) and by the technique (only reproduces with the synthetic bare-Event dispatch, not real navigation). |

## Guardrails

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-035 | `.claude/settings.json` deny list widened beyond `install` subcommands to all of `npm`/`npx`/`yarn`/`pnpm` (Bash and PowerShell), plus writes to `package.json`/lockfiles | APPROVED (Claude) | Claude | Guardrails | Zero-dependency, no-build-step project (CLAUDE.md) — blocking only `install` still permits `npm run`/`npx <tool>` or a hand-authored `package.json` to introduce a build step by the back door. |
| D-039 | `Edit(manifest.json)` / `Write(manifest.json)` deny rule removed from `.claude/settings.json` | APPROVED (Human) | Human | Phase 1 | Owner explicitly overruled the guardrail mid-Phase-1 to let Claude wire `utils/debugLogger.js` into the content-script load order. The rule blocked all manifest edits unconditionally (not just `permissions`/`host_permissions`, which is what S1 actually protects); removing it doesn't relax S1 itself — permission/host_permission changes still require a STOP. |
| D-051 | `chrome-devtools-mcp` (npm-launched Chrome DevTools Protocol MCP server) added to **user-scope** `~/.claude.json` — not this repo's `.mcp.json` — with `--categoryExtensions=true`, and used for Phase 7's live verification instead of the `claude-in-chrome` extension | DONE | Claude | Guardrails | Owner asked for the checklist to actually be run, not just described. `claude-in-chrome` hit two hard blockers: (1) it refuses to navigate/interact with `chrome-extension://` pages and its screenshots only cover the page viewport, not the toolbar — so the settings popup and `chrome.storage.local` were unreachable; (2) in that tool's Chrome instance, `<video>` never buffered at all (`readyState` stuck at 0, zero `videoplayback` network requests) regardless of which video was loaded — unrelated to the extension. `chrome-devtools-mcp` has no such restriction (raw CDP) and its Chrome instance plays video normally. User-scope, not project-scope, because it's a personal verification tool, not something this zero-dependency repo should carry as project config (keeps D-035's spirit). Requires an actual session restart after editing `~/.claude.json` for the new MCP tools to load — cannot be hot-reloaded mid-session. |
| D-052 | Verification technique for Phase 7 (and future runtime-behavior phases): drive the extension's popup page directly via `chrome-devtools-mcp`'s `install_extension` + a `new_page` to `chrome-extension://<id>/popup/popup.html`, read/write `chrome.storage.local` from *that* page's script context (not the YouTube tab's), and precede any resume/save assertion with a `navigate_page{type:"reload", initScript:...}` that installs a `setInterval`-based sampler recording `{t, currentTime, toast, button}` into `window.__samples` from t=0 of the new document | DONE | Claude | Guardrails | Three real traps this avoids, discovered empirically: (1) content-script **isolated worlds don't share prototypes** with the page's main world — a `HTMLMediaElement.prototype.currentTime` setter monkey-patch from `evaluate_script` never sees the extension's own seeks, so don't bother; (2) each tool-call round-trip in this environment cost tens of real seconds, comfortably longer than the toast's ~2.2s and the Restart button's 7s auto-dismiss windows — sampling via a *separate* follow-up `evaluate_script` call after `wait_for`/`navigate_page` reliably arrives too late and finds both already dismissed; recording via the page's own `setInterval` (started by `initScript`, read back whenever convenient) sidesteps this entirely; (3) letting the video **actually play** between setting up a precondition (`video.currentTime = X; video.pause()`) and reloading is unsafe even with a `play`→`pause()` guard listener — YouTube's autoplay resumed it enough during that latency gap to overwrite the saved position via `pagehide`. Writing the `youtubeResume` precondition **directly to storage** from the popup context (bypassing live playback entirely) instead of trying to pin a real seek is the reliable way to set up "previously watched to Xs" scenarios. |
| D-138 | `.claude/settings.json` gains `"env": {"CLAUDE_CODE_MAX_OUTPUT_TOKENS": "32000"}`, doubling the default 16k output-token cap | APPROVED (Claude) | Claude | Guardrails | Owner reported two dead Phase-3 sessions that repeatedly hit `API Error: Claude's response exceeded the 16000 output token maximum` and stalled with no commits. Root cause is almost certainly CLAUDE.md's own token-discipline rules being violated in-session (a full-file rewrite or echoed command output emitted in one response instead of targeted `Edit`s / piped, truncated output) rather than any single task genuinely needing >16k tokens of output — so the real fix is holding to those rules; this cap raise is a safety margin, not a license to emit large blocks. |
| D-154 | `chrome-devtools-mcp`'s user-scope config (`~/.claude.json`, D-051) gains `--chromeArg=--mute-audio`, so every tab/window in that dedicated debugging Chrome profile launches silenced | DONE | Human | Guardrails | Owner-requested after live Phase 4 verification (D-153) played real YouTube video/ad audio during an automated session. `--mute-audio` is Chrome's own native flag (not a chrome-devtools-mcp-specific one); the CLI plumbs it through via the documented `--chromeArg` passthrough (repeatable, one Chrome arg per occurrence — confirmed by reading `chrome-devtools-mcp`'s own source: `config/browser-options.js`'s `chromeArg` option feeds directly into `browser.js`'s `puppeteer.launch({ args: [...options.chromeArgs, ...] })`), not a guess at an undocumented flag. Only applies when chrome-devtools-mcp itself launches Chrome (a `--browserUrl`/`--wsEndpoint` attach-to-existing-browser session is unaffected — not this project's setup). **Takes effect on the next fresh browser launch only** — per D-051's existing note, `~/.claude.json` MCP config changes need a session restart to load, and this specifically also needs the *browser* to actually restart (killing the existing `chrome.exe` for that profile, or simply waiting for the next `install_extension`/`new_page` after a full Claude Code restart) since an already-running Chrome instance keeps whatever flags it was launched with. |

## v3.0.0 — Roadmap & scope (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-063 | v3.0.0 is a **single release** covering defect fixes (Phases 0–3) and pinning (Phases 4–6), with no interim ship gate | APPROVED (Claude) | Claude | All (v3) | Prompt specified this explicitly; no split-release option was offered. Structural constraint: Phases 0–3 must not depend on anything introduced in Phases 4–6, so they stay independently releasable if pinning is ever deferred (docs/ROADMAP_v3.md §1). |
| D-064 | Video ID is the sole identity for a saved entry; title/thumbnail are display-only metadata, never used in lookup or key derivation | DONE | Claude | Phase 1 (v3) | Reading the shipped `storageManager.js` shows `videoId` is already the only key in use — Phase 1 makes this structurally explicit (audit + guarding comment) rather than assuming a bug exists, since Phase 0 may find defects A/B have a different root cause. |
| D-065 | Storage repair (migration chain, duplicate-merge pass, unresolved-videoId rejection) is non-destructive: no entry is ever deleted by Phase 2's logic, only merged or rejected pre-write | DONE | Claude | Phase 2 (v3) | CLAUDE.md S2 protects existing users' saved data; a merge bug that deletes real progress would violate that even if unintentional, so the roadmap states the non-destructive guarantee as an explicit constraint (2.5) rather than an implicit hope. Built and verified live: T2.2/T2.3/T2.7 confirmed the merge pass only collapses duplicate rows for the same video, never a distinct one; T2.4/T2.5 confirmed rejected writes leave the store untouched. |
| D-066 | Progress tracking starts disarmed on every page load and arms only after the resume lifecycle resolves; a large backwards write is rejected unless preceded by an observed user seek | DONE | Claude | Phase 3 (v3) | Targets defect (C) structurally (a near-zero overwrite) without assuming Phase 0 finds one specific trigger — the gate is general-purpose. Explicitly does not change the fixed 400ms delay (CLAUDE.md hard constraint) or the existing verified-seek retry from v2.0.0 Phase 2 (D-022), only when tracking is allowed to write at all. Built and live-verified this session (see ROADMAP_v3.md Phase 3 Findings): T3.1–T3.8 pass. |
| D-067 | Pin cap is 20; pinned entries are fully exempt from the 200-entry eviction cap (the cap applies to unpinned entries only); a 21st pin attempt is refused, never auto-unpinned | DONE | Claude | Phase 4 (v3) | Tier 2 value pick, consistent with the existing philosophy of small fixed limits (200-entry cap, six settings, no free numeric input). Auto-unpinning to make room was explicitly ruled out per the prompt — a silent unpin would be a surprising, user-invisible data change. Built and live-verified this session — see D-093. |
| D-068 | Schema advances to v3 (adds optional `pinned: boolean`, default `false`); migration becomes version-aware (chain-based) instead of a single "write current version" check, so each step is idempotent independently | APPROVED (Claude) | Claude | Phase 2 & 4 (v3) | Purely additive per CLAUDE.md's schema constraint. Version-aware chaining is needed because Phase 2 also wants migration idempotency as a fix in its own right (defect-adjacent robustness), not just to land Phase 4's new field. Superseded in part by D-071 below, which splits *which phase* actually bumps the version number. |
| D-069 | Defect B's primary hypothesis revised to legacy v1-era data surviving the v1→v2 migration and being re-imported/re-merged into `youtubeResume` because migration cannot reliably detect it already ran; the title-participates-in-identity theory is demoted to secondary | APPROVED (Claude) | Claude | Phase 0 (v3) | Owner correction: the legacy-reimport theory explains all three reported facts at once (200-entry cap exactly filled, every entry titled "Untitled video" since v1 stored no titles, and onset specifically on browser restart), where the title-identity theory only explains the titling and is already weakened by `storageManager.js` keying `youtubeResume` by `videoId` alone. Phase 0 now tests both explicitly rather than assuming either. |
| D-070 | Storage repair's non-destructive rule (D-065) gets one named exception: after a *verified successful* migration, the legacy v1 key/shape may be removed so it cannot be re-imported — but only if Phase 0 confirmed D-069's legacy-reimport hypothesis, and never touching a v2/v3 `youtubeResume` entry | **REJECTED — retired, unsupported** | Claude | Phase 2 (v3) | Owner-amended instruction: drop this task outright. Its precondition never held — D-085/0.5 found no code path in any shipped version that ever created a second/legacy storage key, and 0.0's live export of the owner's real profile found none either. There is nothing for this exception to remove and never was; not implemented in Phase 2, and not to be resurrected without new evidence of a legacy key actually existing. |
| D-071 | Schema version 3 is introduced **exclusively in Phase 4** (pinning); Phase 2's version-aware migration chain (D-068) builds the mechanism but leaves `youtubeResumeSchema` at its pre-existing value. Phase 2/3 alone therefore never advertise or require a v3 shape | DONE | Claude | Phase 2 & 4 (v3) | Owner correction: Phase 2.1 and Phase 4.1 both bumping to v3 independently would mean a user on Phases-0–3-only (per the independent-releasability constraint, D-063) could land on schema 3 without ever gaining pinning support, then skip the real v3 step entirely on a later upgrade. Assigning the version bump to exactly one phase removes the collision. Phase 4 landed the actual bump (`CURRENT_SCHEMA_VERSION` 2→3) this session — see D-093. |
| D-076 | "Clear all saved videos" removes only the `youtubeResume` root key (PRD §7.4), so it would not have removed a surviving legacy v1-era key living elsewhere — the original defect-B artifact is therefore **not confirmed gone**. Phase 0 now runs a read-only live-profile inspection (task 0.0) against the owner's real profile *before* any synthetic reproduction, on the chance it is the only surviving copy of the actual failure state | APPROVED (Claude) | Claude | Phase 0 (v3) | Owner correction: the D-069 legacy-reimport hypothesis assumes a second, undocumented key survives migration; if true, the one storage action the owner has taken (clear-all) would not have touched it either, since clear-all's own scope is deliberately limited to `youtubeResume` (D-014's separate-root-key design working as intended, here with an unintended side effect on diagnosability). Ordering synthetic repro (0.5–0.8) after 0.0 avoids overwriting or never discovering a real artifact in favor of a reconstruction. |
| D-096 | Closed a gap D-080 specified but Phase 5 never built: `popup.js`/`popup.html` gained a `pinnedCount` variable (seeded from the initial storage read, kept in sync on pin/unpin toggle and on deleting a pinned row, reset to 0 after `clearAllProgress`) and a `<p id="confirm-pinned-note">` element that shows CP-66 (plural)/CP-67 (singular) whenever `pinnedCount > 0` when the "Clear saved progress" confirmation panel opens | DONE | Claude | Phase 5/6 (v3) | CLAUDE.md's "a doc is wrong or contradicts the code… fill it" latitude applied to a docs-reconciliation phase finding a real, user-visible copy gap rather than a doc inaccuracy: D-080 (UX Spec, Tier 2, already `APPROVED`) named the exact copy and trigger condition, and no Phase 5/6 task list ever named it as a code deliverable, so it shipped without ever being flagged missing until this pass's `grep` for `pinnedCount`/`CP-66`/`CP-67` found zero matches in `popup.js`. Built and verified by reading the resulting `popup.js`/`popup.html` directly (not just the diff) before writing any doc claiming it done. |
| D-097 | Found via docs reconciliation, then fixed same session despite Phase 6 being nominally docs-only: the pin-limit-reached inline message (CP-65, `.pin-cap-message` in `popup.js`'s `showPinCapMessage`) had no `aria-live`, so a screen-reader user got no announcement when a 21st pin attempt was refused, contradicting UX Spec §8.3's prior claim that it was announced | DONE | Claude | Phase 6 (v3) | Found via `grep` for `aria-live` across `popup.js`/`popup.html` (zero matches) while verifying §8.3 against shipped code — not new to pinning: the same pre-existing v2.0-era gap also applied to `#confirm-panel`/`#reset-confirm-panel` (§8.3's separate "Confirmation copy" row made the identical `aria-live="polite"` claim, equally false). Fixed both in the same pass rather than left as documented gaps: `role="status"`/`aria-live="polite"` added to `.pin-cap-message` (`popup.js`) and to `#confirm-panel`/`#reset-confirm-panel` (`popup.html`) — attribute-only additions with no layout/behavior change, well within Tier 1/2 and CLAUDE.md's "fill it" latitude, so shipping the fix in the same phase that found it beat leaving two known, trivial accessibility bugs on the books. UX Spec §8.3's pin-limit and confirmation-copy rows now both correctly state the announcement happens. |

## v3.0.0 — UX Spec update (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-077 | Pin affordance splits into two elements: a persistent, non-interactive pinned badge on the thumbnail (state visible without hovering) and a separate hover-revealed, keyboard-focusable pin/unpin toggle in the row's action area, drawn as inline SVG (not emoji), matching the Ko-fi icon precedent (D-058) | APPROVED (Claude) | Claude | Phase 5 (v3) | Satisfies both Roadmap v3 Phase 5.1 (control revealed on hover, like the existing remove control) and 5.3 (pinned state visible without hovering) without contradiction — one element shows state, the other changes it. |
| D-078 | Sort order becomes two-tier: pinned entries first (most recent within that group), then unpinned entries (most recent within that group); still not user-configurable | APPROVED (Claude) | Claude | Phase 5 (v3) | Direct specification of Roadmap v3 Phase 5.2 in UX-Spec terms; no new setting introduced — pin/unpin is the only lever a user has over ordering. |
| D-079 | Pin-limit-reached (21st pin attempt) is a refusal shown as a brief inline message (CP-65) in the row's action area, auto-dismissing after ~2.5s — no `alert()`/`confirm()`, no auto-unpin | APPROVED (Claude) | Claude | Phase 5 (v3) | Roadmap v3 5.4 required "no silent no-op" and "no alert/confirm dialog" but left the exact presentation open; 2.5s matches the resume toast's existing brevity convention (§5) rather than inventing a new timing value. |
| D-080 | "Clear saved progress" confirmation (CP-49/CP-50, unchanged) gains a conditional appended line — CP-66 (plural) or CP-67 (singular) — naming the pinned count explicitly whenever at least one video is pinned | APPROVED (Claude) | Claude | Phase 5/6 (v3) | Pinning protects only against the 200-entry cap, not against this explicit user action (Roadmap v3 4.5); Copy Rule 4 (§2.3, "confirmation dialogs state the action plainly") means a user who assumes pinning is absolute protection must be told otherwise before confirming, not after. |
| D-081 | Missing-title display reuses the existing CP-37 fallback (`Untitled video`) unchanged; v3.0 only clarifies that the fallback is no longer necessarily permanent, per the Roadmap v3 Phase 2.4 lazy-backfill fix — no new copy ID needed | APPROVED (Claude) | Claude | Phase 2 (v3 docs) | Mirrors the existing channel-name omission behavior already documented in PRD §7.3; adding a distinct "not yet backfilled" string would imply a different visual state than a title that was simply never captured, which isn't the case — both render identically as CP-37 until the next successful save. |
| D-082 | Task 0.0's live-profile finding takes precedence over T0.8's synthetic result; a three-outcome table added to Roadmap v3 Phase 0 makes explicit that (a) 0.0 finding legacy data stands regardless of T0.8, (b) 0.0 finding nothing plus T0.8 passing refutes the hypothesis, (c) 0.0 finding legacy data plus T0.8 passing means migration previously left data behind even though current code doesn't reproduce it — Phase 2 must still neutralise it | APPROVED (Claude) | Claude | Phase 0 (v3) | 0.0 reflects the owner's actual upgrade history under whatever migration shipped at the time; T0.8 only proves today's code is clean, not that a past profile is. Rewrote task 0.0 to run headless-first: Claude Code inspects a static export the owner pastes in (via a one-time popup-console copy-to-clipboard snippet), since Claude's CDP-driven Chrome can't reach the owner's real profile. |
| D-083 | PRD NG8 amended (not deleted or renumbered) with a bounded exception: pinning is curation, but only of retention and ordering, never of an entry's content (title/channel/thumbnail/position) | APPROVED (Claude) | Claude | Phase 4/5 (v3 docs) | NG8 as originally worded ("not curation") is literally false of pinning; scoping the exception narrowly to retention/ordering keeps the actual prohibition (no editing/renaming content) intact. |
| D-084 | `popup/popup.html` gains a `<script src="../utils/debugLogger.js">` include (was missing) | DONE | Claude | Phase 0 | Discovered live: Phase 0's new `storageManager.saveProgress()` diagnostic log (0.2) threw `debugLogger is not defined` when called from the popup context, since popup.html only ever loaded `storageManager.js`. Latent gap — no shipped popup code calls `saveProgress` today — but broke this session's own diagnostic testing and would break the moment it did. `debugLogger.log` no-ops with `DEBUG=false`, so this is not a behavior change. |
| D-085 | Phase 0 findings recorded: defect A not reproduced, defect B primary (legacy reimport) refuted (no such code path exists or ever has, confirmed by 0.0's live export finding no extra key and 0.5/T0.6-T0.8's static+live confirmation that `migrate()` never touches `youtubeResume`), defect B secondary (title-identity) not reproduced, defect C **confirmed** (`saveProgress()` unconditionally overwrites, no guard distinguishes a spurious near-zero read from a genuine seek) | DONE | Claude | Phase 0 | Full evidence in `ROADMAP_v3.md`'s "Phase 0 Findings" subsection. Per D-082's evidence-precedence table, 0.0-finds-nothing + T0.8-passes = outcome (b), refuting the primary hypothesis outright rather than deferring to Phase 2's D-070 exception. |
| D-086 | Phase 1 executed as a confirmatory audit rather than a bug fix: every `videoId`-reading call site (`storageManager`, `navigationManager.checkAndEmit()`, `resumeManager`, `progressTracker`, `bootstrap.js`) already keyed/compared by `videoId` alone before this phase, matching D-064's prediction and Phase 0's 0.7/T0.2/T0.3 finding. Landed an explicit identity-invariant comment at `storageManager`'s `store[videoId] = entry` line (code + TDD §4.6 pseudocode) so a future edit can't silently reintroduce title into the key path, plus a TDD §4.6 narrative paragraph and a PRD §7.3 invariant statement. T1.2 (repo-wide grep) returned zero matches; T1.3/T1.4 verified live via the popup console against the built extension (two distinct `videoId`s with an identical title produced two entries; the same `videoId` saved twice with different titles produced one entry, second title winning) | DONE | Claude | Phase 1 | No behavior change — the invariant already held. This closes the failure class structurally per the roadmap's "cheap insurance" framing, independent of Phase 0's A/B(secondary) not-reproduced verdicts. |
| D-087 | `saveProgress`'s and the new merge pass's preserve-if-omitted logic (D-045) now treats a whitespace-only `title`/`channel` string the same as an omitted one, trimming before the truthiness check instead of a bare `title ? … : existing` test | DONE | Claude | Phase 2 (v3) | Roadmap v3 2.4 asked Phase 2 to verify D-045's write-back guard holds under every save trigger and close any gap found. Live testing found one: `saveProgress(id, t, d, '   ', '   ')` is truthy under the original check, so it silently overwrote a real stored title/channel with blank-looking text. Unreachable via real playback today — `youtubeUtils.getTitle()`/`getChannelName()` both trim and return `null` on failure, never a whitespace-only string — but the guard itself needed to be robust to it regardless of caller, since a display fault that gets written back is permanent data loss per the task's own framing. Re-verified live post-fix: the same call now correctly preserves the existing title/channel. |
| D-088 | Read-path investigation for defect B's third hypothesis ("Untitled video" entries were the owner's real entries rendered without titles, not new ones) found no reproducible mechanism; no fourth hypothesis proposed | DONE | Claude | Phase 2 (v3) | `popup.js` awaits `Promise.all([getAllProgress(), getSettings()])` before building any row, and `getAllProgress()` is a single `chrome.storage.local.get` call — Chrome's storage API returns a coherent snapshot, never a torn read of the object inside a key, so there is no code path where rendering can start before the read fully resolves. The one theoretical interleaving (`storageManager.js`'s own load-time `migrate().then(repairDuplicates)` writing concurrently with `popup.js`'s independent read) can only land before or after that write, never mid-write, and the write itself never clears a title (D-045/D-087). Per the task's own instruction, ships the write-back guard (D-087) regardless of this null result rather than inventing a fourth hypothesis. |
| D-090 | Backward-jump write guard: interval-triggered saves only, threshold `BACKWARD_JUMP_THRESHOLD_S = 30`; re-assert-once native-override check in `resumeManager` uses `NATIVE_OVERRIDE_CHECK_DELAY_MS = 500` | DONE | Claude | Phase 3 (v3) | Tier 2 value picks (Roadmap 3.4/3.6 specify the mechanism, not the numbers). 30s matches the existing `minWatchSeconds` default and defect C's observed magnitude (500→2); 500ms is deliberately shorter than the seek-verify window so a native override is caught while still "shortly afterward" per the roadmap wording. No separate "recent user seek" flag was needed for the interval guard's exemption clause — a real backward seek's own `seeked`-triggered save already updates `lastSavedTime` to the new position before the next interval tick runs, so the exemption falls out of existing state rather than needing new state. |
| D-091 | `bootstrap.js` now calls `progressTracker.start()` (disarmed) *before* attempting resume, not after; `progressTracker.arm()` is called in a `finally` block wrapping the resume attempt | DONE | Claude | Phase 3 (v3) | Roadmap 3.1/3.2 require write-blocking during the resume attempt itself, which needs the tracker's event listeners already attached (so a resume-triggered `seeked` event is caught and silently dropped by the disarmed gate, not left unhandled). Reordering, not new logic — the resume attempt and its `.catch()` behavior are unchanged. |
| D-092 | `uiInjector.js`'s Restart button click handler calls `progressTracker.notifyExternalReset()` immediately after forcing `video.currentTime = 0`, resetting the backward-jump guard's `lastSavedTime` baseline to 0 | DONE | Claude | Phase 3 (v3) | Roadmap 3.6 asked to verify whether the Restart path's programmatic seek needs an explicit exemption rather than relying on the generic `seeked`-event one. Live testing found it does: without this call, replaying the restarted video past `minWatchSeconds` reproduced a false-positive rejection (the first post-restart save looked like a huge backward jump from the stale pre-restart `lastSavedTime`). Reproduced and confirmed fixed live this session (ROADMAP_v3.md Phase 3 Findings, T3.8). |
| D-089 | Roadmap v3 Phase 2's "Docs to Update" line erroneously cited "TDD §4.6 … §7.5, §7.6" — the TDD's actual §7 is "Error Handling Strategy" (only §7.1/§7.2 exist); §7.5/§7.6 are PRD section numbers, already listed separately as "PRD §7.6" on the next line. Corrected in the same pass to drop the erroneous TDD reference | APPROVED (Claude) | Claude | Phase 2 (v3 docs) | Shipped-doc-numbering beats a stale cross-reference (CLAUDE.md: fix the doc in the same commit that finds it wrong). Likely a copy-paste artifact from drafting the PRD and TDD update lists side by side. |
| D-093 | Phase 4 built exactly per roadmap: `saveProgress` preserves an existing entry's `pinned` flag across a normal save; eviction filters to unpinned keys before both the >200 count check and the removal-candidate sort; `pinProgress`/`unpinProgress` added and exposed from `storageManager`; migration chain gains its `{ to: 3 }` step, `CURRENT_SCHEMA_VERSION` bumped 2→3 | DONE | Claude | Phase 4 (v3) | No open value picks — Roadmap v3 4.1–4.6 and D-067 (20-pin cap, no auto-unpin, retention-only eviction exemption) already specified the mechanism and every number. Self-verified via a Node `vm` harness loading the real `storageManager.js` against a mocked `chrome.storage.local` (T4.1–T4.8 all pass: schema-2 and schema-1 profiles both land cleanly on 3 with no entry rewrite beyond the version key; 20 pins succeed, a 21st is refused with the 20 unchanged; 20 pinned + 200 unpinned + 1 new unpinned save evicts only an unpinned entry, all pins survive; unpin-then-pin-another returns to exactly 20; `clearAllProgress` removes pinned entries too; 5 reloads are a no-op after the first) — `chrome-devtools-mcp`/real-browser verification wasn't needed since no DOM/UI surface changed this phase. |
| D-094 | Fixed a repeat of the D-089 doc-numbering bug: Roadmap v3 Phase 4's "Docs to Update" line also cited "TDD §4.6 … §7.3, §7.5, §7.6", attributing PRD section numbers to the TDD again. Dropped the erroneous TDD references in the same pass | APPROVED (Claude) | Claude | Phase 4 (v3 docs) | CLAUDE.md: fix the doc in the same commit that finds it wrong. Same root cause as D-089 — the PRD and TDD update lines were evidently drafted side by side per phase and the erroneous copy wasn't caught for Phase 4 when Phase 2's was. |
| D-095 | Phase 5 built exactly per roadmap and the already-planning-approved UX Spec §6.3/D-077/D-078/D-079 (no new value picks needed — pin control placement, sort order, 2.5s cap-message timing, and CP-62/63/65 copy were all already specified): pin/unpin control (outline vs filled `push_pin` SVG toggle) sibling to the remove control, persistent pinned badge on the thumbnail, two-tier sort, and a per-row `data-pinned`/`data-updated`-attribute re-insertion scheme so pinning moves exactly one DOM node instead of rebuilding the list | DONE | Claude | Phase 5 (v3) | Self-verified live via `chrome-devtools-mcp` against a seeded 200-entry/20-pinned profile: T5.1–T5.2 (pin/unpin moves the row to the correct group boundary, storage matches DOM after each toggle), T5.3 (21st-pin attempt at cap shows CP-65 verbatim, no entry pinned, `pinBtn` briefly hidden then restored), T5.4 (link/pin/remove are three independently-tabbable siblings in that DOM order, native `<button>`/`<a>` semantics), T5.5 (badge presence is the pinned/unpinned visual distinguisher, independent of hover), T5.6 (200 entries/20 pinned rendered in ~19ms via a `MutationObserver`-timed reload, comfortably under the 200ms budget), T5.7 (repo-wide `innerHTML` grep: zero matches outside `docs/`), T5.8 (CP-62/63/65 strings match UX Spec §7 verbatim). One bug caught by this same live testing and fixed before considering the phase done: the pin-toggle handler updated the button's ARIA state and moved the DOM node but never wrote back `li.dataset.pinned`/`updated`, so a *second* toggle on any row read stale sort-key data and mis-sorted — found because the first live pin/unpin round-trip test produced inconsistent aria-label-vs-dataset state. |

---

## v3.0.0 — PRD update (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-072 | PRD §5.10 (new) states the three v3 defects as symptom + guaranteed post-fix behaviour only, explicitly deferring mechanism to the roadmap/TDD since Phase 0's root-cause investigation is not yet complete | APPROVED (Claude) | Claude | Pre-Phase-0 (v3 docs) | Matches the doc-routing rule (CLAUDE.md: PRD wins on intent/scope, TDD on implementation detail) — writing a mechanism into the PRD now would risk contradicting whatever Phase 0 actually finds. |
| D-073 | Data-durability guarantee ("a saved position is never replaced by a near-zero position the user did not cause") promoted to Goal G13 and stated as an explicit product promise in PRD §5.10, not left implicit in the roadmap's write-guard tasks | APPROVED (Claude) | Claude | Pre-Phase-0 (v3 docs) | Owner-specified. Makes the guarantee testable at the product level (R16) independent of whichever specific write-guard mechanism Phase 3 implements. |
| D-074 | Pinned videos specified fully in PRD §5.11 (pin/unpin, sort-to-top, 200-cap exemption, 20-pin cap, clear-all still removes pins) as a firm v3.0 requirement, unlike the still-under-investigation defects in §5.10 | APPROVED (Claude) | Claude | Pre-Phase-4 (v3 docs) | Pinning is a decided feature (Roadmap v3 Phases 4–5), not an open investigation, so the PRD specifies its full product behaviour now rather than deferring detail the way §5.10 defers defect mechanism. |
| D-075 | §6.1 project tree and §6.3 manifest snippet corrected to list `utils/debugLogger.js`, which has shipped since v2.0.0 (Phase 1 instrumentation) but was omitted from the PRD's architecture docs | DONE | Claude | v3 docs | Found while verifying §6.1 against the real `manifest.json`. Shipped code beats every doc (CLAUDE.md) — fixed in the same pass rather than filed separately. The "no popup in v1.0" drift named in the prompt was checked against §3.2 directly and not found; §3.2 already correctly states v1.0 shipped a status popup (see the C4 changelog row), so no PRD text needed correcting there. |

## Phase 6 — Doc Reconciliation, Regression & Store Release

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-098 | Phase 6's live `chrome.storage.local` writes via `chrome-devtools-mcp`'s `evaluate_script` (the D-052 technique used successfully in Phases 4/7/9) were auto-denied this session by the harness's Auto Mode safety classifier; substituted an offline Node `vm` harness loading the real `storageManager.js` against a mocked `chrome.storage.local` (D-093's Phase 4 technique) for T6.1 (schema-2→3 upgrade), T6.2 (fresh install), and T6.5's eviction/pin-cap logic, plus read-only live browser checks (console, DOM state, a real zip-install) for what remained observable without a storage write | DONE | Claude | Phase 6 (v3) | Same class of scoping call as D-060 (Phase 9's soak-test substitution): the target behavior (storage layer correctness) is fully covered by the substitute technique, only the specific tool changed. T6.3/T6.4 (full regression, defects A/B/C) were not independently re-run live this session beyond a no-error spot check, since Phase 6 changed no resume/tracking code — they lean on Phase 0–3's already-recorded live verification earlier this same session. T6.5's 20-cold-load live half (mixed pinned/unpinned resumes) was not run live for the same reason; the storage-layer half (eviction/cap correctness under a 20-pinned/200-unpinned profile) was verified via the harness. Flagged in the phase report as a real gap, not silently substituted. **Update:** owner added `mcp__chrome-devtools__evaluate_script` to `.claude/settings.local.json`'s `permissions.allow` list post-phase; a benign probe write/read/remove against `chrome.storage.local` confirmed the classifier no longer blocks this call pattern. The live reruns this row originally deferred (T6.5's 20-cold-load half, a from-scratch T6.1/T6.3 live pass) were not subsequently requested or run — the gap is now tooling-unblocked but still open if ever wanted. `.claude/settings.local.json` is gitignored (user-global `~/.config/git/ignore`), so this permission grant lives locally only, not in the repo. |
| D-099 | Manifest version bumped `2.0.0` → `3.0.0`; no other manifest field changed (`permissions`, `host_permissions`, `content_scripts` load order, icons all byte-identical to v2.0.0) | DONE | Claude | Phase 6 (v3) | Roadmap v3 6.5, a direct instruction with no open value pick. Verified via diff-equivalent field-by-field read of `manifest.json` (6.3/6.4) and a real zip-install-and-load test (T6.7) that showed zero warnings. |

---

## v4.0.0 — Roadmap & scope (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-100 | Roadmap v4's ten-phase structure (0 harness, 1 boundary validation, 2 serialized writer, 3 write ownership/freshness, 4 resume identity/cancellation, 5 verified resume outcomes, 6 deferred recovery, 7 completion policy/cleanup, 8 popup reconciliation/a11y, 9 regression & release) is fixed and maps every audit finding F01–F21 to exactly one phase, with ship gates A/B/C after Phases 3/6/8 | APPROVED (Claude) | Claude | All (v4) | Owner-specified structure, adopted verbatim from the prompt rather than re-derived from the audit's own "Proposed remediation order," which groups findings similarly but not identically (e.g. the audit folds F13/F16/F17 into its cleanup step; this roadmap keeps F13 with the serialized-writer/cap-enforcement phases it structurally belongs to and F16/F17 with popup reconciliation). Audit's order is directional guidance, not a binding phase map. |
| D-101 | Phases 0–3 (harness, boundary validation, serialized writer, write ownership/freshness) remain independently releasable with no dependency on Phases 4–9, mirroring Roadmap v3 §3's constraint; nothing before Phase 4 may reference resume-identity/cancellation state, deferred-recovery state, or the completion marker | APPROVED (Claude) | Claude | All (v4) | Explicit per the prompt. Keeps a storage-correctness-only release shippable on its own (Gate A) if resume-lifecycle or completion work is ever deferred or cut, same rationale as v3. |
| D-102 | A background service worker (`background/storageWriter.js`) becomes the sole writer for every `chrome.storage.local` mutation (saveProgress, deleteProgress, pin, unpin, repair, migration, settings, clearAll); `storageManager.js` keeps its exact current public API and becomes a client that sends commands to the worker; reads stay direct, no round trip. The worker is restart-safe — every command is idempotent and no state lives only in worker memory. `manifest.json` gains `background.service_worker` and nothing else; `permissions`/`host_permissions` are unchanged | APPROVED (Claude) | Claude | Phase 2/3 (v4) | Owner-approved architecture, directly answering F06's "Address" recommendation (route all mutations through one serialized extension-owned writer, e.g. an MV3 service worker) and F06's acceptance criterion (worker restart/retry cases). Restart-safety is load-bearing: MV3 service workers are terminated on idle by the browser at any time, so a command whose only record of having happened lived in worker RAM would silently vanish — every command must be safely re-appliable from `chrome.storage.local` state alone. |
| D-103 | Per-video storage keys (one `chrome.storage.local` key per video, replacing the single `youtubeResume` map) are rejected for v4.0.0 | APPROVED (Claude) | Claude | Phase 2 (v4 scope) | Owner-specified rejection, overriding F06's "Address" text which floats per-video keys as a way to reduce unrelated-key contention. F06's own caveat already concedes they "still need coordination for caps, pinning, clearing, and same-video conflicts" — i.e. they would not remove the serialization requirement, only relocate part of it, while multiplying the number of keys the 200-cap/20-pin-cap logic must reason about and complicating the additive-migration guarantee (CLAUDE.md hard constraint: storage root keys must not change shape). The serialized single-writer approach (D-102) already solves the coordination problem F06 raises without a schema-shape change. |
| D-104 | Completion is a new additive schema field (`ended: boolean`, default absent/`false`) set only when playback genuinely reaches the end (a real `ended` event on content media, not an ad, not a seek-to-end), independent of the resume cutoff (`completionThreshold`). Legacy entries saved before this field existed use a conservative inference rule: an entry with no `ended` field is inferred complete only if `Math.floor(time) >= duration - 1` (i.e. within the smallest unit the existing integer-second storage can represent) — not the 90/95/98% resume cutoff. The popup stops displaying "100%" unless one of these two conditions holds | DONE | Claude | Phase 7 (v4) | Directly resolves F14/F15's core complaint: displayed 100%/the resume cutoff is not a safe completion predicate (Math.round shows 100% at 99.5%; a 98% cutoff still excludes the final 7.2 minutes of a 6-hour video). The `duration - 1` legacy rule is deliberately the strictest defensible reading of "conservative" — anything looser (e.g. 98%) risks exactly the false-positive bulk-deletion F14's acceptance criterion forbids ("do not silently turn a '100%' cleanup request into deletion of everything above 95%"); erring toward under-counting completions (a few genuinely-finished legacy videos not swept up by Remove Completed) is the safe failure direction, since the user can still remove them individually. |
| D-105 | "Remove completed" (new popup action) matches only the completion predicate from D-104, never the resume cutoff. It preserves pinned entries by default, with an opt-in checkbox/toggle to include them; it previews the exact count before acting; it uses the existing inline confirmation pattern (no new dialog primitive); there is no undo window | DONE | Claude | Phase 7 (v4) | Directly resolves F14 (product gap, requested by the user) using F14's own recommended defaults ("recommended default is to preserve pinned videos with a separate opt-in to include them," "preview the exact removal count," "one coordinated batch mutation"). No-undo-window is an explicit scope cut from F14's "an undo window is preferable if practical" — the existing extension has no undo primitive anywhere (single-row remove and clear-all are both immediate), and inventing one exclusively for this action would be a new UI pattern this release does not otherwise need; the preview-before-confirm step is the mitigation the existing inline-confirmation pattern already provides for the single-row/clear-all cases. |
| D-106 | "Treat as finished at" gains a fourth segmented option, "Only at the end," alongside the existing 90/95/98%. No new setting key: `completionThreshold` gains the sentinel value `1` (100%), meaning resume/near-completion suppression triggers only when the D-104 `ended` marker (or its legacy inference) is true, not by any percentage-of-duration comparison | DONE | Claude | Phase 7 (v4) | Owner-specified fourth option and "no new setting key" constraint. `1` is the natural sentinel for "at the end" on a `0–1` fraction field already storing 0.9/0.95/0.98 — every existing comparison site (`savedTime < duration × completionThreshold`) degenerates correctly to "only true resume-suppress at the literal end" when paired with D-104's `ended`-based check replacing the pure arithmetic comparison at that boundary value, so no second field is needed to represent a fifth state. |
| D-107 | An explicit, valid `t=` timestamp parameter in the navigation URL wins for that navigation: it cancels automatic saved-position restoration outright and the resulting playback is treated as a normal user-directed session (eligible for normal interval/event tracking, not specially protected) | APPROVED (Claude) | Claude | Phase 4 (v4) | Directly adopts F20's own "Recommended" policy text verbatim. Resolves the previously undefined precedence F20 identifies (today, behavior "depends on timing rather than an explicit policy") by making the timestamp link authoritative rather than racing it against automatic resume. |
| D-108 | A dependency-free Node regression harness reproducing the audit's R1–R24 scenarios against real, unmodified source is committed to `tests/` (not a one-off session artifact); it is excluded from the Chrome Web Store zip. Phase 0 builds the harness and its R1–R24 baseline; Phases 1–8 extend it with cases for whatever each phase changes | DONE | Claude | Phase 0 (v4), extended per-phase | Directly resolves F21's core finding: "No committed runnable regression suite exists in the reviewed tree" and its own one-off harness note ("the one-off harness itself was not added to the repository"). Dependency-free matches CLAUDE.md's "no build step, no bundler, no dependencies" convention extended to test tooling; store-zip exclusion keeps `tests/` out of the shipped artifact the same way `docs/` already is. Built this session: `tests/lib/` (vm-based harness loading real source against a fake clock/DOM/chrome.storage.local/video element), `tests/cases/r01–r24-*.js`, `tests/run.js`. All 24 cases reproduce against current HEAD — `docs/PHASE0_FINDINGS_v4.md`. |
| D-109 | Explicitly out of scope for v4.0.0, rejected rather than deferred without comment: always-on/interval polling as a resume-recovery mechanism, telemetry of any kind, a global highest-time-wins conflict rule across tabs/sessions, and unbounded seek retry | APPROVED (Claude) | Claude | All (v4 scope) | Owner-specified rejections. Each directly forecloses a tempting-but-wrong fix the audit itself warns against: F02's "Address" explicitly says to reuse the existing scheduling mechanism rather than add polling; F05's "Address" explicitly says "a permanent highest-time-wins rule would break intentional rewinds and is not a suitable fix"; F01/F03's "Address" both call for *bounded* stabilization/retry, not unbounded; telemetry is foreclosed by the standing hard constraint (zero network requests beyond the gated thumbnail GET) and was never on the table. |
| D-110 | New copy IDs for v4.0.0 start at CP-68, continuing from CP-67 (the last ID in use per UX Spec §7, confirmed by repo-wide grep before drafting this roadmap) | APPROVED (Claude) | Claude | Phases 4–8 (v4, wherever new copy is needed) | Owner-specified starting ID; verified rather than assumed — CP-64 was intentionally skipped in v3 (reserved, then unused, per UX Spec §7's own note), so CP-67 and not CP-64 is the correct last-used anchor. |
| D-112 | Unpinning while already at 200 unpinned entries immediately evicts the oldest eligible unpinned entry (by `updated`), rather than allowing the unpinned count to silently exceed 200 until the next `saveProgress()` | DONE | Claude | Phase 1 (v4) | Value pick resolving F13's own open question ("Explicitly define whether unpinning into a full library immediately evicts the oldest eligible entry"). Consistent with the existing eviction rule already used at save time (oldest-by-`updated`) rather than inventing a second rule; closes F13's R20 gap (201 unpinned entries surviving until a later save) at the moment the cap is actually breached instead of deferring it. |
| D-111 | Ship gates: Gate A after Phase 3 (storage correctness — serialized writer, boundary validation, write ownership/freshness), Gate B after Phase 6 (resume reliability — identity/cancellation, verified outcomes, deferred recovery), Gate C after Phase 8 (product completeness — completion policy, popup reconciliation/accessibility). Phase 9 (regression, docs, store release) follows Gate C and is not itself a gate | APPROVED (Claude) | Claude | All (v4) | Owner-specified gate placement, given a name and rationale here since the prompt states the gates without one. Each gate lands after a coherent, independently-valuable slice (storage safety, then resume correctness, then product features) rather than at arbitrary phase boundaries, so a real-world release decision to ship early has a defensible cut point at each gate. |
| D-113 | PRD v4.0.0's schema v4 additions are named `ended` (matches Roadmap v4 7.1/D-104 verbatim), `revision` (a persisted counter, since D-102 requires the worker to hold no state that lives only in worker memory, and Roadmap v4 §3.4/§4 describe a revision counter bumped on delete/write), and `owner` (an opaque, display-only identifier of the last-writing tab/session) | APPROVED (Claude) | Claude | Phase 3/7 (v4) | Value pick filling a PRD-level gap: Roadmap v4 describes revision counters and session/generation tokens as worker mechanism (TDD territory) but never names persisted `VideoProgress` fields, and the PRD is instructed not to describe mechanism. `owner` is deliberately described at the data-shape level only (an identifier, display-only, never used for lookup) so the PRD states what exists without describing how session/generation identity is established or validated — that stays for the TDD. |
| D-114 | PRD v4.0.0 extends three places beyond the prompt's explicit list, for internal consistency: §5.8's "Treat as finished at" options row now lists the new "Only at the end" option (the prompt named it only for §5.10/changelog); §6.2's `storageManager.js`/`storageWriter.js` rows are reconciled so the table no longer says `storageManager.js` is "the only module that touches `chrome.storage.local`" while also introducing a dedicated writer; §14.1's "no background script required" key technical constraint is corrected, since v4.0 adds one | APPROVED (Claude) | Claude | Phase 2/3/7 (v4) | Doc-consistency gap-fills per CLAUDE.md's "a doc is wrong or contradicts the code/plan — fix it, log Tier 2" — leaving any of the three unedited would have made the PRD self-contradictory the moment a reader crossed sections. |
| D-115 | `docs/project-state-summary.md`'s doc-versions line is updated to PRD 4.0.0 alongside this PRD revision, even though the task's own verification checklist listed only `PRD_YouTube_Resume.md` and `DECISIONS.md` as expected to change | APPROVED (Claude) | Claude | N/A (docs housekeeping) | The task's own "STATE UPDATE" section explicitly instructs this edit; treated as the authoritative instruction over an apparently incomplete verification checklist, consistent with CLAUDE.md's requirement to keep `project-state-summary.md` accurate every session rather than stall on the discrepancy. |

---

## v4.0.0 — UX Spec update (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-116 | "Remove completed" placed as a text button in the saved-videos list header (next to the count), not in Settings | DONE | Claude | Phase 7 (v4) | Tier 2 value pick. It operates on the currently-listed data (F14's own framing: "clearly discoverable... in the saved-videos view"), unlike the six tunable preferences and the two library-wide destructive actions, which are settings-scoped by design (D-008). |
| D-117 | Displayed percentage for a non-completed row is capped at **99**, not 98 or a lower value | DONE | Claude | Phase 7 (v4) | Tier 2 value pick resolving F15's "cap approximate percentages below 100" without a specified number. 99 is the smallest visual change from today's `Math.round()` behavior that still satisfies "never claims 100% from playhead position alone" — anything lower would understate genuinely-far-along videos with no added honesty benefit, since the completion marker (D-104), not the percentage, is what actually gates the completed label (CP-77). |
| D-118 | The Remove-completed "include pinned" choice (CP-73) is a per-action checkbox that resets to unchecked every time the popup opens, not a persisted setting | DONE | Claude | Phase 7 (v4) | Tier 2 value pick. D-105 specifies "opt-in to include them" but not persistence; defaulting to unchecked every time keeps the safer pinned-preserving behavior the default on every single invocation, consistent with pinning's "protects retention" intent (D-083) — a persisted opt-in could silently stay on across sessions and defeat that intent without the user re-affirming it. |
| D-119 | Remove-completed's zero-match state **disables** the button (with CP-74 shown in place of the count) rather than hiding it | DONE | Claude | Phase 7 (v4) | Tier 2 value pick, matching F14's "clearly discoverable" requirement — a hidden control isn't discoverable, and a hidden control that reappears once a video completes would be a surprising UI change with no trigger the user witnessed. Mirrors the existing pin-limit-message inline-swap pattern (D-079) rather than inventing a new disabled-state convention. |
| D-120 | Remove-completed confirm button gets new copy **CP-79 (`Remove`)**, distinct from CP-51 (`Clear`); Cancel reuses the existing generic **CP-52** unchanged | DONE | Claude | Phase 7 (v4) | New copy with no assigned ID, per CLAUDE.md's "never stop for this — decide instead." `Remove` matches the action's own verb (Copy Rule 3, active voice) better than reusing `Clear`, which this popup already uses specifically for "erase everything." Cancel's copy has no action-specific meaning to preserve, so reusing CP-52 avoids a needless duplicate ID. Logged Tier 3 (new user-facing copy) per CLAUDE.md. |
| D-121 | The load-failure state (CP-75/CP-76) triggers only when the **progress** read fails or returns unrenderable data — not when the **settings** read fails | DONE | Claude | Phase 8 (v4) | Directly resolves F12's explicit requirement ("settings read failure does not hide valid saved videos") without new mechanism: the popup already falls back to safe setting defaults on a settings-read failure (existing behavior), so only a progress-read failure has no existing safe fallback and needs the new distinct state. |
| D-122 | CP-43h's helper copy is updated in place (same ID) to `Videos watched past this point won't resume. "Only at the end" waits until the video actually finishes.` | APPROVED (Claude) | Claude | Phase 7 (v4) | This update's own task explicitly authorized changing CP-43h if it became inaccurate — the original wording ("past this point") no longer describes the fourth option (D-106), which gates on the completion marker, not a percentage-of-duration point. Logged Tier 3 (changed user-facing copy) despite the narrow, task-granted exception to the standing "never reword an existing CP ID" rule — every other existing ID in the document is untouched. |
| D-123 | Focus-handoff destinations for row removal (next row, else previous row, else the Remove-completed button, else the settings gear), pin re-sort (control retains focus across its DOM move), and confirmation open/cancel (focus moves to Cancel on open, returns to the trigger on cancel) are specified explicitly | APPROVED (Claude) | Claude | Phase 8 (v4) | Tier 2 value picks filling F18's "move focus predictably" without naming exact targets. Ordering (next row before previous row before header controls) mirrors the most common list-removal UX convention and keeps focus inside the list whenever a neighboring row exists. |
| D-124 | The in-player toast's `prefers-reduced-motion` handling shows/removes it at full opacity for the same ~2200ms total window, rather than skipping the animation but also shortening the timing | APPROVED (Claude) | Claude | Phase 5/8 (v4) | Tier 2 value pick, F18's requirement scoped to "decorative animation" generically. Keeping the visible-duration budget unchanged avoids re-opening §5.4's already-measured timing (D-046) just to satisfy a motion preference that only concerns the transition, not the duration. |

## v4.0.0 — Working-instructions reconciliation (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-125 | CLAUDE.md's hard constraint amended from "only `storage/storageManager.js` touches `chrome.storage.local`" to: `storageManager.js` and `background/storageWriter.js` may both touch it, `storageManager` as the sole read path and `storageWriter` as the sole write path, with every other module going through `storageManager`'s public API. CLAUDE.md retargeted to v4.0.0 in progress (v3.0.0 shipped baseline) with corrected doc versions and the 200-unpinned/20-pinned retention wording; TDD §1/§1.2/§2/§4.6 updated to state the v4 service-worker architecture (header bumped to 4.0.0-draft, not 4.0.0, since only those sections were touched) | APPROVED (Claude) | Claude | Phase 2/3 (v4) | Directly implements D-102's approved architecture into the two working documents that would otherwise still forbid it and describe it as absent, so building Phase 2/3 as approved would look like a hard-constraint violation and a TDD contradiction under CLAUDE.md's own "shipped code beats every doc" / "a doc is wrong — fix it" rules. No new permission, no schema change, no code written this pass. |
| D-126 | The Chrome Web Store zip is built from an explicit include-list of shipped paths (`manifest.json assets content storage utils popup background`) rather than an exclude pattern, so `tests/` and `docs/` are left out by construction, never listed, instead of relying on a fragile exclude rule staying in sync as directories are added | APPROVED (Claude) | Claude | Phase 0 (v4, documented); Phase 9 (v4, executed) | Roadmap v4 Phase 0 task 0.7 required noting the exclusion for Phase 9 to act on; no zip build script or documented procedure existed anywhere in the repo before this phase (`docs/Dev_Checklist_YouTube_Resume.md` §12.6 only checklists "zipped and loadable," no command) — filled the gap now (Tier 2 value pick) rather than leaving Phase 9 to invent one ad hoc. Procedure documented in `docs/ROADMAP_v4.md`'s Phase 0 Findings subsection; `background/` is listed pre-emptively for Phase 2's planned service worker (D-102) and is harmless to list before it exists (`zip` warns and skips a missing path, doesn't fail). |

## v4.0.0 — Phase 1 (Boundary Validation & Safe Repair)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-127 | Unresolved-key/malformed-value quarantine and the pre-merge repair log (Roadmap v4 1.2/1.4) share one new, additive root key `youtubeResumeQuarantine`: `{ entries: { [rawKey]: { entry, reason, quarantinedAt } }, repairLog: [{ rawKey, canonicalId, before, after, at }] }` (`repairLog` bounded to the last 20, newest first). Never read by resume/tracking/popup code, never auto-emptied by this phase | DONE | Claude | Phase 1 (v4) | Tier 2 value pick — the roadmap specified the behavior ("quarantined," "reversible pre-repair snapshot, bounded local retention") but not the storage shape. One key rather than two keeps the additive-root-key footprint minimal; CLAUDE.md's storage-root-key list updated in the same commit (D-125's own precedent — fix the doc when it's found incomplete). |
| D-128 | `getSettings()` validates each field against an explicit allowed-preset set (`minWatchSeconds`: 10/30/60/120; `completionThreshold`: 0.9/0.95/0.98/**1**; `rewindSeconds`: 0/2/5/10) or an actual-`boolean`-type check, falling back per-field to the default rather than merging the stored object over defaults wholesale | DONE | Claude | Phase 1 (v4) | Closes R18/F12. `completionThreshold`'s `1` is pre-validated now even though Phase 7 (D-106) is what actually wires the "Only at the end" option — the roadmap's own task 1.6 names it explicitly ("90/95/98% + Phase 7's future fourth value"), and validating it early costs nothing since no UI can write it yet. |
| D-129 | `resolveVideoId()`'s substring-match fallback (the root cause of R16/F11's identity-truncation bug) is removed outright rather than narrowed; the only two "proven, not guessed" fallbacks (Roadmap 1.1) accepted are surrounding whitespace and two explicit URL forms (`youtube.com/watch?v=...`, `youtu.be/...`) | DONE | Claude | Phase 1 (v4) | Tier 2 mechanism pick — the roadmap named "an explicitly supported URL form" as allowed without specifying which; these two are the only URL shapes `youtubeUtils.getVideoId()` and the rest of the codebase already recognize as YouTube watch URLs, so no new URL vocabulary is introduced. |
| D-130 | `mergeEntryPair()`'s `pinned` field uses OR semantics (either side pinned ⇒ merged entry pinned), and any field neither `time`/`duration`/`updated`/`title`/`channel`/`pinned` survives a merge via an object-spread base rather than being named explicitly | DONE | Claude | Phase 1 (v4) | Closes R15/F11 (previously `pinned` was never copied at all). OR, not "recent entry wins," because pin protection is a user-set guarantee that neither side of an accidental duplicate should be able to silently revoke by being the "more recent" row. The spread base is forward-looking for schema fields Phase 7 (`ended`, `revision`, `owner`) will add, so a future merge doesn't need to be re-taught about them. |
| D-131 | The already-assigned UX Spec §7 copy CP-75/CP-76 (load-failure state, D-121) is built now, in Phase 1, rather than deferred to Phase 8 as D-121's own `Implement In` column says | DONE | Claude | Phase 1 (v4) | This phase's own prompt (roadmap 1.7/T1.6) explicitly requires a distinct progress-read-failure state, and the copy for it already exists verbatim in UX Spec §7 — building it now isn't new copy needing an ID, just an earlier build date than D-121 originally planned. D-121 itself is left as-is (append-only ledger); this row is the reconciling note. Logged Tier 3 since it's new user-visible popup behavior (`#load-failure-state`, `popup.html`/`popup.js`). |

## v4.0.0 — Phase 2 (Serialized Storage Writer)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-132 | Pure schema/validation/repair logic (constants, `sanitizeEntry`, `sanitizeSettingsValue`, `resolveVideoId`, `mergeEntryPair`, `enforceUnpinnedCap`, `repairStore`, the migration step list) factored out of `storageManager.js` into a new `storage/storageValidation.js`, loaded by both `storageManager.js` (a manifest/popup.html `<script>`) and `background/storageWriter.js` (via `importScripts()`, since a classic non-`"module"` service worker can do that) | DONE | Claude | Phase 2 (v4) | Roadmap 2.4/2.6 require one validation choke point, not duplicated per call site — a real dependency once reads (storageManager) and writes (storageWriter) run in separate JS realms with no bundler to merge them (CLAUDE.md: no build step). Kept under `storage/`, not `utils/`, purely to avoid reordering CLAUDE.md's existing "storage -> utils -> content" manifest load order around a new cross-cutting dependency — `storageManager.js` needs it at its own load time (destructured at the top of its IIFE), which `utils/` loading after `storage/` in the current order would break. |
| D-133 | Command set: `SAVE_PROGRESS`, `DELETE_PROGRESS`, `PIN`, `UNPIN`, `CLEAR_ALL`, `SET_SETTINGS`, `REPAIR`, `MIGRATE` (roadmap 2.2's list) plus a ninth, `RESET_SETTINGS` | DONE | Claude | Phase 2 (v4) | `resetSettings()` is a distinct existing public mutation (v2.0.0 Phase 6, full-replace-with-defaults) the roadmap's illustrative eight didn't name — filled the gap (CLAUDE.md: task list has a gap, fill it) with its own command rather than overloading `SET_SETTINGS` with a reset flag, keeping each command's semantics ("apply fully from scratch") unambiguous. |
| D-134 | Messaging client: one long-lived `chrome.runtime.connect({name:'storageWriter'})` port (re-created lazily whenever missing), requests correlated by an incrementing id. `sendCommand()` retries only a *transient* failure (the port disconnected, or `postMessage` threw because it was already dead) with bounded backoff `[100, 300, 900]` ms (three attempts); an application-level rejection (worker responded, just refusing — e.g. pin cap reached) is never retried | DONE | Claude | Phase 2 (v4) | Tier 2 value picks for the retry schedule/bound (roadmap 2.7 specifies the requirement — "the calling function's promise rejects cleanly" on an unreachable worker — not the numbers). Distinguishing transient-vs-application failures matters: retrying "pin cap reached" three times can't change the outcome, only delay reporting it by up to ~1.3s for no benefit. |
| D-135 | `storageManager.js` opens its connection to the worker once on load (`getPort()`, fire-and-forget, wrapped in try/catch) rather than only lazily on the first mutating call | DONE | Claude | Phase 2 (v4) | Roadmap 2.5/task instruction ("await writer readiness before any mutation," migration/repair "not once per tab or popup instance") — connecting alone wakes a dormant MV3 service worker in real Chrome, so this mirrors the pre-Phase-2 per-load `migrate().then(repairDuplicates)` self-invocation closely enough that Phase 1's repair/quarantine cases (T1-quarantine-visible, R15-R18) keep passing without any mutating call in their setup, while now running once per *worker* lifetime instead of once per tab/popup instance (the actual F06-relevant race the old code had). |
| D-136 | Worker startup (`migrate()` then `repairDuplicates()`, moved verbatim into `storageWriter.js`) runs as the serialized queue's first link (`let queueTail = runStartup()...`), so it implicitly gates every command without a client-side readiness handshake; it reruns once per worker *process* lifetime (an MV3 worker can be spun up and idled out many times per browser session), not literally once ever | DONE | Claude | Phase 2 (v4) | Interprets the task's "migration and repair run exactly once through the worker" as "once per worker instantiation, not once per tab/popup" (the actual bug being fixed) rather than "exactly once, ever" (unenforceable across worker restarts and unnecessary, since both operations are already idempotent by construction — D-065/D-068). |
| D-137 | Harness support for Phase 2 (`tests/lib/fakeRuntime.js`, `tests/lib/harness.js`/`popupHarness.js` updates): models `chrome.runtime.connect()`/`onConnect` as paired in-process `Port` objects delivered asynchronously via the shared fake clock, with `_terminateWorker()`/`restartWorker()` test-only controls that disconnect all live ports and re-execute `storageWriter.js` into the same vm context. Both `storageManager.js` and `storageWriter.js` load into one shared vm context per test (real Chrome runs them in separate realms) | DONE | Claude | Phase 2 (v4) | A faithful two-realm simulation (separate vm contexts communicating only by serialized messages) was judged not worth the added harness complexity for what these cases actually need to prove — command serialization and idempotent client-side retry — since the two modules only ever interact through the fake `chrome.runtime` port, never by reaching into each other's closures, so a case can't pass by accident via shared state that wouldn't exist in real Chrome. T2.1-T2.4 (via R13/R14/R19 flipping to not-reproduced, plus a new concurrent-pin-cap case) and T2.5 (worker-termination-mid-command retry) all pass against this harness; T2.6/T2.7 verify the manifest and public-API surface directly from disk, independent of the harness. |

## v4.0.0 — Phase 3 (Write Ownership, Freshness & Durable Saves)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-139 | Reverses D-113's schema-shape prediction: v4.0.0's schema does **not** gain persisted `revision`/`owner` fields after all. The write-ownership/freshness check (F07/R23) instead compares each save's session-local `lastActiveAt` (ms, never persisted) against the existing entry's already-persisted `updated` timestamp — no new field needed. PRD §7.3/§7.4/§7.6/C22/R26 corrected in the same commit (only `ended` bumps the schema, exclusively in Phase 7, mirroring how v2→v3's `pinned` bump landed exclusively in its own phase per D-071's v3.0.0 precedent) | APPROVED (Claude) | Claude | Phase 3 (v4) | D-113 was a docs-only planning-pass prediction, made before Phase 3's actual mechanism was designed. Roadmap v4's own Phase 3 task list (3.1-3.7) and Exit Criteria never call for a schema bump — only Phase 7's task 7.1 does, for `ended` — and this session's own explicit instruction is "schema bump if and only if Roadmap v4 specifies it." Per CLAUDE.md precedence (TDD > UX Spec > Roadmap > PRD for implementation detail), the roadmap's silence on a Phase 3 schema bump overrides the PRD's earlier, more specific prediction. The existing `updated` field already carries everything the freshness check needs (a wall-clock moment to compare a session's own last-active time against); adding `revision`/`owner` on top would have been unused surface area with no code path reading either name. |
| D-140 | `progressTracker`'s single `lastSavedTime` splits into `committedTime` (updates only on a write's successful, non-stale acknowledgement — Roadmap 3.5), `lastAttemptedTime` (updates synchronously the instant any write is issued, preserving the pre-existing backward-jump-guard exemption for a real rewind's own `seeked`-triggered save even before that save's ack lands), and `dirty` (true from issue until acknowledged-and-current, so an unacknowledged attempt's interval retry isn't mistaken for "nothing changed" by the delta guard — closes R24/F10). `writeSeq`/`lastAckedSeq` reject a stale, out-of-order acknowledgement from regressing `committedTime` (T3.5) | DONE | Claude | Phase 3 (v4) | Tier 2 mechanism pick filling Roadmap 3.5/3.6's behavioral spec ("separate observed/pending/committed positions," "dirty sample for bounded retry," "reject an out-of-order acknowledgment") without a named implementation. A single ack-gated marker alone would have fixed R24 but broken the existing backward-jump-guard exemption for genuine rewinds (a real seek's own save not yet acknowledged would make the very next interval tick look like a spurious 3000+ second drop) — `lastAttemptedTime` is what keeps that pre-existing guarantee intact while still gating the delta-guard/retry logic on actual acknowledgement. |
| D-141 | `storageManager.saveProgress()` gains a 6th, optional `ownership` parameter (`{ sessionId, lastActiveAt, explicitUserSeek, trigger }`), carried through to the writer as new `SAVE_PROGRESS` payload fields. Backward compatible: every field defaults via `?.` when omitted, so T2.5's pre-Phase-3 call site (no ownership arg) is unaffected. T2.7's expected arity for `saveProgress` updated from 5 to 6 with an explanatory comment, rather than treating the intentional signature extension as a regression | DONE | Claude | Phase 3 (v4) | The freshness check (D-139) needs the calling session's own activity/intent metadata, and there's exactly one caller of `saveProgress` in the codebase (`progressTracker`) — extending its signature is lower-risk than inventing a parallel "ownership-aware save" function. T2.7 exists specifically to catch an *unintended* API drift (Phase 2's own purpose); amending its expectation for a documented, intentional Phase 3 change is the correct response per CLAUDE.md's "a test fails — diagnose and fix it," not leaving it permanently red. |
| D-142 | Every `'seeked'` event sets this session's `hasUserSeek = true`, sent as `explicitUserSeek` on every subsequent save from that session — including a later, unrelated pause/hidden event in the same session. Disambiguating a genuine user seek from YouTube's own native-override `seeked` firing is explicitly left to Phase 5 (5.8: "reconcile the `seeked`-triggered save's exemption... with Phase 3's freshness rules") | APPROVED (Claude) | Claude | Phase 3 (v4) | Roadmap 3.3 requires an "explicit user-seek flag" without specifying how it's detected, and Phase 5's own task list already names this exact disambiguation as its own future work — building it here would duplicate work Phase 5 does properly (keyboard/pointer/accessible-control interaction, not just the `seeked` event in isolation, per 5.4). Treating every `seeked` as explicit is the simpler interim reading the roadmap's own phase sequencing anticipates. |
| D-143 | Deletion/clear-all "freshness" state (`deletionRevisionAt: Map<videoId, msEpoch>`, `globalRevisionAt: number`) lives only in `storageWriter.js`'s in-memory module scope — never persisted to `chrome.storage.local`, never a new root key. A worker restart between a deletion and a stale tab's next lifecycle event loses that specific video's anti-resurrection protection until fresh activity re-establishes ownership normally | APPROVED (Claude) | Claude | Phase 3 (v4) | Roadmap 3.4 itself says the revision counter is bumped "in the worker," read most naturally as worker-scoped ordering state rather than a durability requirement — the same framing D-102's own file header already uses to justify the command queue itself being memory-only ("not itself a durability boundary... a fresh worker instance simply starts a fresh, empty queue"). Treated as the same class of state here: losing it on restart reintroduces a narrow, bounded regression to pre-Phase-3 behavior for one video in one race window, never data loss or corruption (the actual saved entry, once written, is unaffected). A persisted alternative was considered and rejected: writing a tombstone row into `youtubeResume` itself would make `getAllProgress()` return a ghost entry to the popup (violates the "additive, non-destructive, no new root key unless justified" bar this phase set for itself, D-139), and a new dedicated root key for a handful of short-lived timestamps was judged disproportionate to the narrow race it protects against. Documented as an accepted, bounded exception rather than left unstated. |
| D-144 | Gate A (F06, F07, F10, F11, F12, F13) confirmed closed: full harness run (`node tests/run.js`) shows R13/R14/R15/R16/R17/R18/R19/R20/R23/R24 and every T1/T2/T3 case `not-reproduced`; only F01-F05/F08/F09/F03 cases (Phase 4-6 scope) still show `reproduces`, as expected. Grep for Phase 4+ concepts (`generation`, `cancellation`, `attemptId`) in the touched files found only doc references noting the Phase 3/4 boundary, no implemented mechanism | DONE | Claude | Phase 3 (v4) | Roadmap v4 Exit Criteria for Phase 3 explicitly require this dual check (all listed findings fixed, and no reference to any Phase 4+ concept) before Gate A can be considered closed. |

| D-145 | Phase 3 live-verified via `chrome-devtools-mcp` this session (D-051/D-052 technique, popup-context direct calls): a stale-session save (`lastActiveAt` 5 minutes old, no `explicitUserSeek`) against a fresher existing entry is rejected; the same stale session with `explicitUserSeek: true` succeeds; deleting an entry then attempting a save from an unchanged/stale session does not recreate it; a normal fresh-session save→pin→unpin→delete→save→clear-all round trip all succeed; only network request observed was the expected `i.ytimg.com` thumbnail GET (404, since the smoke-test video ID isn't real — expected, not a violation). **Gotcha for future sessions:** `install_extension` on a path already installed in a prior turn can start the worker from a stale cached copy even after source files changed on disk — `reload_extension` was required before the freshness check actually reflected the current `storageWriter.js`; don't trust a first install after mid-session edits without an explicit reload | DONE | Claude | Phase 3 (v4) | Closes CONTEXT's "GATE A CHECK" instruction (harness plus live smoke of resume/tracking/pin/unpin/remove/clear-all). The stale-cache gotcha cost real debugging time this session (a live rejection test initially, wrongly, appeared to fail) — recorded so a future phase's live verification doesn't repeat it. |

## v4.0.0 — Phase 4 (Resume Identity & Cancellation)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-146 | The Phase 4 generation token lives entirely in `bootstrap.js` as a single monotonically increasing `currentGeneration` counter, one per `onVideoChange()` call, rather than threading a separate token/id through `progressTracker`'s existing Phase 3 `sessionId`. `resumeManager.tryResume()` receives an `isCurrent()` closure instead of a raw generation number | DONE | Claude | Phase 4 | Roadmap 4.1 says "extend Phase 3's generation/session token to cover the full navigation lifecycle" without naming a mechanism. `progressTracker.sessionId` is Phase 3's storage-ownership concept (compared server-side, in `storageWriter.js`, against another *tab's* session) — a different axis from Phase 4's need (compare a navigation against *itself*, synchronously, within one tab). Reusing the same field would conflate two different staleness questions; a second, purpose-built counter at the one place all the gated actions (seek/save/arm/UI) already funnel through (`bootstrap.js`'s pipeline) is simpler and race-free by construction in single-threaded JS. See TDD §5.1 for the full contract. |
| D-147 | `playerObserver.disconnect()` settles a pending `waitForVideo()` by rejecting it (not resolving with a sentinel), closing R12 | DONE | Claude | Phase 4 | The audit's own framing ("leaves it unresolved forever") only requires *some* settlement; rejection was chosen over a null-resolve because every existing caller (`bootstrap.js`) already wraps the await in try/catch and treats failure as "player initialization failed, skip silently" — matching the existing timeout-rejection code path exactly, so no new caller-side branching was needed. |
| D-148 | F04/R5's fix reorders `tryResume()` to defer through ads and resolve real content metadata (new `establishContentMetadata()` helper) *before* `shouldResume()`, rather than only moving the existing ad-check earlier in place | DONE | Claude | Phase 4 | Roadmap 4.4 specifies the outcome (eligibility never runs against ad duration) but not the mechanism. Extracting a dedicated function (also reused for the mid-verification revalidation call, D-149) was chosen over inlining the reorder twice, since the audit's F04 evidence and Roadmap 4.5 explicitly call for the *same* ad-defer-then-metadata-wait logic to run again if an ad interrupts verification. |
| D-149 | Mid-verification ad interruption (an ad starting during the 400ms delay or its round-loop) triggers a full `establishContentMetadata()` re-run once the ad clears; the seek is abandoned (not just re-delayed) if the revalidated `video.duration` differs from the duration eligibility was originally decided against by more than 1 second | DONE | Claude | Phase 4 | Roadmap 4.5 ("revalidate metadata if the media source or ad state changes... a second ad starting"). 1 second tolerance is a Tier 2 value pick — tight enough to catch a genuine source swap (an ad's duration vs. real content's duration differ by orders of magnitude in every audit example) while not falsely tripping on sub-second timing noise between two reads of the same content's duration. |
| D-150 | `waitForAdClear()` takes an `isCurrent` callback and checks it on every `AD_POLL_MS` (250ms) poll tick, resolving early instead of running to the full 60s ceiling when a navigation is torn down mid-wait | DONE | Claude | Phase 4 | Roadmap 4.2 ("teardown... settles any pending promise tied to the old generation — player discovery, metadata wait, ad wait, seek"). A recursive `setTimeout` chain (not a new `setInterval`) keeps this compliant with CLAUDE.md's one-interval-alive constraint. `waitForMetadata()`'s existing bounded ~10s (5s + one retry) timeout was left as post-hoc-gated rather than given the same tick-level cancellation — its ceiling is already an order of magnitude below the ad wait's 60s, and CLAUDE.md's anti-overengineering guidance argues against adding machinery to shave a already-small bound further when nothing incorrect can happen while it elapses (the `isCurrent()` check right after already prevents any seek/save/UI from a superseded generation). |
| D-151 | F04's "SPA reuse can expose previous-content metadata" evidence is addressed structurally: `bootstrap.js` tracks the `<video>` element and its `duration` seen at the start of the previous navigation; if a new navigation resolves the *same* element with the *same* non-NaN duration, `tryResume()` is told to give it one bounded `REUSED_ELEMENT_REFRESH_MS` (1000ms) chance at a real `loadedmetadata` event before trusting the value, rather than either always forcing a wait (would add latency/timeout risk to the common fast-refresh case) or never checking (the original gap) | DONE | Claude | Phase 4 | Not explicitly named as a mechanism by any roadmap task, but directly required to make T4.4's pass condition ("the new generation confirms ownership before using any existing metadata") true for the harder case where the carried-over value is non-NaN and so looks superficially valid — an `isNaN`-only check (which the pre-existing `waitForMetadata()` already had) misses it entirely. 1000ms is a Tier 2 value pick: comfortably longer than a same-tick DOM update, comfortably shorter than making a legitimately-fast resume feel delayed. A timeout here is never treated as failure — only as "no new metadata event fired, use what's present" — since a false-positive match (two different videos coincidentally sharing an exact duration) must not cost a real resume. |
| D-152 | New harness cases added this phase — `t4-rapid-abc-navigation.js` (T4.1), `t4-leave-during-ad.js` (T4.2), `t4-reused-media-element.js` (T4.4), `t4-spa-short-to-long.js` (T4.7) — cover the roadmap's four scenarios beyond what R21/R22 (already-existing two-navigation cases) exercise. T4.3/T4.5/T4.6 were judged adequately covered by R5's existing case plus D-149's revalidation logic without a dedicated new file | APPROVED (Claude) | Claude | Phase 4 | CLAUDE.md's phase-end instruction to run the phase's test table via the harness where possible, extending it rather than only asserting behavior manually. All four new cases pass; full suite (`node tests/run.js`) shows zero regressions across all 24 R-cases and every existing T-case. |
| D-153 | Live browser verification via `chrome-devtools-mcp` completed on retry once the profile lock cleared. Real pre-roll ad on `dQw4w9WgXcQ` (two consecutive ads, video.duration briefly 31.101s/32.721s): `tryResume` never evaluated eligibility against the ad's duration, correctly deferred through both ads via `establishContentMetadata`, and cleanly abandoned at the 60s ceiling once the ad break itself exceeded it (`ad-timeout`, D-020's pre-existing bound — not a regression, no false success shown, `progressTracker.arm()`d normally afterward). A second, ad-free video (`M7lc1UVf-VE`) resumed end-to-end: seek to 58s verified with 0 drift, native-override check clean, toast/button shown, interval save committed with correct title/channel. Rapid synthetic SPA navigation A→B→C (`dQw4w9WgXcQ`→`9bZkp7q19f0`→`kJQP7kiw5Fk`, `history.pushState` + dispatched `yt-navigate-finish`) produced zero wrong-ID or stale saves — direct storage reads confirmed A and B's seeded entries were completely untouched throughout, only the final (C) navigation's entry was ever updated, and one interval save attempt from a stale prior session was itself rejected by Phase 3's write-ownership check (`saveProgress rejected: stale session`), confirming both phases' mechanisms compose correctly live | DONE | Claude | Phase 4 | Closes the one open item from this phase's self-verification. **Caveat, documented rather than hidden:** the synthetic navigation dispatch used a bare `Event` (not YouTube's own `CustomEvent` with `detail.endpoint`), which throws inside YouTube's own router (`Cannot read properties of undefined (reading 'endpoint')`, identical to the D-062 precedent) and means the real `<video>` element's underlying content never actually swapped between A/B/C — only the URL and our extension's own tracking did. This is a test-methodology limitation, not a code gap: it still fully exercised generation-token teardown/cancellation and the reused-element metadata-freshness check (D-151, which correctly triggered given the same element + carried-over duration), and the storage-level outcome (writes attributed only to the current generation) is exactly what T4.1/R21/R22 assert. A real multi-tab-click rapid-switch test would need the owner's own interactive session. `DEBUG` was flipped to `true` for this session only and reverted to `false` before finishing (confirmed via `git status` showing zero diff on `utils/debugLogger.js`) — never committed true, per CLAUDE.md. |

## v4.0.0 — Phase 5 (Verified Resume Outcomes)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-155 | Outcome taxonomy is seven statuses — `verified, pending, deferred, cancelled, user-overridden, ineligible, failed` — reconciling Roadmap 5.6's simpler four-status sketch (`success \| failed \| cancelled \| ineligible`) with the phase prompt's fuller list. `resumeManager.tryResume()` returns `{status, reason, target, observed, attemptId}`; `resumeManager.OUTCOME` exposes the status constants. `deferred` is defined but not yet produced by any Phase 5 code path — reserved for Phase 6's Deferred Recovery Lifecycle, which owns resuming a session that never settled within this phase's bounded window | DONE | Claude | Phase 5 | Doc conflict between the roadmap task and the fuller prompt spec — the fuller taxonomy was chosen since it's strictly more expressive and the roadmap's own test table (T5.1–T5.13) already implies distinctions (e.g. "pending" vs "failed" vs "cancelled") the 4-status sketch can't carry. Tier 2. |
| D-156 | New `utils/userIntent.js` module tracks the most recent `keydown`/`pointerdown`/`mousedown` on `document` (capture phase) via `wasRecentInput(windowMs)`, loaded after `utils/timeUtils.js` and before `content/playerObserver.js` in both `manifest.json` and the test harness | DONE | Claude | Phase 5 | F08's fix requires distinguishing genuine user intent from native interference; no existing module tracked input recency. A dedicated, dependency-free module (no DOM beyond `addEventListener`, no storage, no network) was chosen over inlining the listeners into `resumeManager`/`progressTracker` separately, since both need the same signal and CLAUDE.md's `utils/` convention is pure/shared logic. `lastInputAt` is seeded to `-Infinity`, not `0` — `0` is a legitimate `Date.now()`/fake-clock timestamp at the very start of a session, and a falsy-zero sentinel would read as "no input ever happened" forever in exactly that case (caught by `tests/cases/t5-deliberate-rewind-corroborated.js` initially failing until fixed). |
| D-157 | `seekWithVerification()`/`isPositionSettled()` (F01/R1) require `!video.seeking` and `video.readyState >= HAVE_CURRENT_DATA (2)` in addition to the existing 3-second drift tolerance before counting a seek as settled | DONE | Claude | Phase 5 | Direct implementation of task 5.1; `HAVE_CURRENT_DATA` is the standard `HTMLMediaElement` readiness constant, not an invented threshold. |
| D-158 | The fixed-magnitude/forward-only resume-delay guard is replaced by `checkStabilization()`: direction-agnostic drift computed against elapsed time and `video.playbackRate` (0 expected drift while paused), gated on `userIntent.wasRecentInput(800ms)` — corroborated drift cancels resume (`cancelled`/`user-seek`); uncorroborated drift is treated as native interference and the resume proceeds anyway, overriding it | DONE | Claude | Phase 5 | Direct implementation of tasks 5.3/5.4. 800ms was chosen as a Tier 2 value: comfortably wider than one JS event-loop tick so a `keydown`/`pointerdown` immediately preceding the position change is still "recent," comfortably narrower than risking attributing an unrelated later interaction to this specific jump. |
| D-159 | Post-verification native-override monitoring (F01/R2) runs as a bounded, fire-and-forget background poll (`monitorForLateOverride()`: `MONITOR_ROUNDS = 4` × `MONITOR_INTERVAL_MS = 500`, ~2s total) launched after a `VERIFIED` outcome, not awaited as part of `tryResume()`'s own promise chain | DONE | Claude | Phase 5 | Task 5.1 says "monitor for a native override after the current 500ms check" without specifying duration or whether it blocks. Making it a detached background task was necessary for correctness, not just a style choice: `bootstrap.js`'s `arm()` must not wait on it (R2's test fires the late override only *after* `tryResume()` has already resolved), and CLAUDE.md's "no fighting YouTube indefinitely" ruled out an unbounded watch. 4 rounds/2s is a Tier 2 value pick — long enough to catch a native restore cue's typical delay (observed live in Phase 4 as a few hundred ms to ~1s), short enough to stay clearly bounded. Stands down immediately on `userIntent.wasRecentInput()`, never overriding a genuine user action. |
| D-160 | The existing 500ms reassert check (`reassertIfNativeOverride`, D-090) is renamed `verifyAndReassert()` and now returns a typed `{ok, reason, userOverride?}` instead of swallowing a thrown corrective re-seek (F01/R3): a throw is `FAILED`/`reassert-threw`, never a success toast. A drift corroborated by `userIntent` at this checkpoint is reported as `USER_OVERRIDDEN`, distinct from `VERIFIED` — no success UI either way, since the position is no longer the one the extension is claiming to have restored | DONE | Claude | Phase 5 | Direct implementation of task 5.2/R3's fix; `USER_OVERRIDDEN` as its own status (vs. folding it into `VERIFIED` or `CANCELLED`) follows the prompt's explicit 7-status taxonomy (D-155). |
| D-161 | `progressTracker.attemptSave()` gains a third parameter `bypassBackwardJumpGuard` (default `= bypassDelta`, preserving existing behavior for `pause`/`ended`/`visibility`/`pagehide`); the `'seeked'` handler now passes `userIntent.wasRecentInput(SEEK_CORROBORATION_WINDOW_MS = 2000)` instead of always exempting itself (F05/R8) | DONE | Claude | Phase 5 | The pre-existing single `bypassDelta` flag conflated two different guards for every event trigger — R8 is exactly that conflation for `'seeked'` specifically (a native jump immediately followed by a synthetic `'seeked'` saved straight through the backward-jump guard). Splitting the flag rather than removing `'seeked'`'s exemption outright was required by the hard constraint "Deliberate rewind... keep working" — a real scrubber drag landing well above `minWatchSeconds` must still save, so the exemption is now conditional on real input corroboration rather than absolute. 2000ms (vs. `resumeManager`'s 800ms window, D-158) is a separate Tier 2 pick: a real scrubber drag can take a couple of seconds from first touch to the eventual `seeked` event, materially longer than the tight native-vs-user distinction needed during the 400ms resume delay. |
| D-162 | `handleSeeked` resets `lastAttemptedTime` to the new position on every `'seeked'` event — including one `attemptSave` rejected for `belowMinWatch` — except when the result was `'backwardJumpRejected'` and uncorroborated (F08/R9) | DONE | Claude | Phase 5 | Direct implementation of task 5.5. The single exception (leave the baseline untouched on an uncorroborated rejection) is required to not undo D-161's protection in the same breath — resetting unconditionally would let the very next interval save through at the native-jumped position. |
| D-163 | `progressTracker.protectCheckpoint(time)` (new) seeds `committedTime`/`lastAttemptedTime` to a known saved checkpoint's time instead of the actual (low) startup position; `bootstrap.js` calls it between `start()` and `arm()` whenever a resume attempt for an existing saved checkpoint did not reach `VERIFIED` or an established user-directed outcome (F05/R7). `arm()` itself stays unconditional in the `finally` block — CLAUDE.md's graceful-degradation rule requires tracking to keep working after a failed resume, so the fix protects the checkpoint rather than withholding arming | DONE | Claude | Phase 5 | Task 5.7 describes the outcome ("preserved through a pending/recoverable failure, released only after a verified success or an established user-directed session") without naming a mechanism. Reusing the *existing* backward-jump guard (rather than adding a second, parallel protection concept) was chosen because it requires no new guard logic — only re-seeding what the guard already compares against — and composes cleanly with D-161/D-162's changes to the same guard. `tests/cases/r07-arm-after-failed-resume-overwrites-checkpoint.js` was rewritten to exercise the real `bootstrap.js` pipeline (`loadBootstrap: true` + a real navigation) instead of calling `progressTracker.start()`/`arm()` directly, since the original test's direct-API call *was* the old unconditional-arm bug pattern — asserting the fix requires exercising bootstrap's new decision logic, which the isolated-API test bypassed entirely. |
| D-164 | `progressTracker.markUserDirected()` (new) sets `hasUserSeek = true` and calls `recordActivity()` without touching position baselines; `bootstrap.js` calls it for a `t=` timestamp navigation (no native `'seeked'` DOM event occurs there) and for a resume outcome that establishes user-direction (`USER_OVERRIDDEN`, or `CANCELLED`/`'user-seek'`) | DONE | Claude | Phase 5 | Found live in this phase's own test-writing: `tests/cases/t5-timestamp-over-checkpoint.js` initially failed because storageManager's Phase 3 write-ownership/freshness check (D-066) rejected the timestamp session's first interval save as "stale" — `lastActiveAt` (seeded at `start()`) predated the seeded checkpoint's `updated` value, and nothing had set `explicitUserSeek` (no native `seeked` event fires for a `t=` load, since nothing is seeked *away from*). Explicit marking from `bootstrap.js`, rather than relying solely on the native handler's side effect, also covers the resume-outcome case without depending on event-ordering between the video's own `seeked` firing and this decision. |
| D-165 | **Timestamp precedence (D-107/F20) implemented by *not calling* `resumeManager.tryResume()` at all** for a navigation carrying a valid `t=` value — `bootstrap.js` checks `youtubeUtils.getTimestampSeconds()` (new, `utils/youtubeUtils.js`) right after `progressTracker.start()`, and if non-null, marks the session user-directed, arms immediately, and returns, skipping the storage read and resume pipeline entirely | DONE | Claude | Phase 5 | Roadmap 5.10 only requires that the timestamp "cancel automatic resume" — it does not require the extension to *perform* the timestamp seek itself, and YouTube's own player already does that natively from the URL. Having the extension also seek would risk exactly the competing-seek race the task is trying to prevent. `getTimestampSeconds()` parses plain-seconds (`90`, `90s`) and compound (`1h2m3s`-subset) forms, returning `null` (fail closed) for anything malformed — an invalid value must never be mistaken for a valid precedence signal (T5.12). |
| D-166 | New test files this phase: `tests/cases/t5-native-override-stages.js` (T5.4), `t5-buffering-several-seconds.js` (T5.4 buffering variant), `t5-storage-read-failure.js` (T5.7), `t5-timestamp-over-checkpoint.js` (T5.10), `t5-invalid-timestamp.js` (T5.12), `t5-user-seek-cancels-resume.js` (T5.5), `t5-deliberate-rewind-corroborated.js` (T5.6, regression guard for D-161/D-162 against the "deliberate rewind must keep working" hard constraint). `tests/cases/r04-forward-jump-cancels-resume.js`'s `advance()` budget raised 1000ms→2000ms (the fixed behavior now runs the full ~1150ms seek+verify+reassert pipeline instead of aborting early, so the old budget under-ran the new — correct — completion time) | APPROVED (Claude) | Claude | Phase 5 | CLAUDE.md's phase-end instruction to extend the harness per the phase's test table. T5.4's buffering variant deliberately asserts a `pending` outcome (not eventual `verified`) — multi-second buffering exceeds the bounded ~1150ms seek-verify window by design (D-022's "never an unbounded retry loop"); resuming *after* buffering clears is Phase 6 scope, not fabricated here. T5.13 (playlist navigation) was judged adequately covered structurally by Phase 4's existing generation/identity mechanism (a playlist advance is just another `onVideoChange()`) without a dedicated new case. |

| D-167 | Live browser verification via `chrome-devtools-mcp` (D-051/D-052 technique) completed for Phase 5's owner-facing checks. On a real pre-roll-ad video (`dQw4w9WgXcQ`, precondition written directly via the popup's `storageManager.saveProgress`): eligibility correctly evaluated against the real 213s content duration (not the 15s ad's), resume seeked to the saved checkpoint once the ad cleared, and the toast/Restart button appeared only on the verified outcome — no premature or missing UI. A real corroborated rewind (`mousedown` + `currentTime` set backward 140s to 40s, well above `minWatchSeconds`) saved correctly despite Phase 5's new backward-jump guard on `'seeked'`, confirming the "deliberate rewind keeps working" hard constraint held live, not just in the harness. A `t=30s` navigation over an existing 160s saved checkpoint showed no toast/button, started playback from the timestamp (never the checkpoint), and the very next interval save persisted the new low position over the old checkpoint without being rejected as an overwrite risk — confirming timestamp precedence and `markUserDirected()`'s freshness-override both work end-to-end | DONE | Claude | Phase 5 | The buffering/slow-readiness live check (roadmap's own "Live" bullet) was not independently reproduced this session — reliably forcing multi-second `readyState` buffering on a real, unthrottled connection isn't practical via this tooling, and the scenario is already covered deterministically by `tests/cases/t5-buffering-several-seconds.js`. All test-provided storage preconditions were deleted after verification; nothing left behind in the profile. |

## v4.0.0 — Phase 6 (Deferred Recovery Lifecycle)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-168 | Recovery trigger set: `visibilitychange` (tab becoming visible) and `pageshow` (bfcache/back-forward restore), both wired once in `bootstrap.js`'s `init()`, calling a shared `attemptRecovery()` that is a no-op unless a `pendingRecoveryVideoId` is actually set and the current URL still names that same video. No new timer or polling loop — these are page-lifecycle events, not a beat-driven check | DONE | Claude | Phase 6 | Roadmap 6.1's "visibility return, pageshow, other relevant page-lifecycle events" without naming the exact set. `pendingRecoveryVideoId` is set only when `playerObserver.waitForVideo()` times out entirely for the still-current generation (bootstrap's outer catch) — a session that ever gets further than player discovery never sets it, so 6.4 ("don't re-seek after already-successful playback") holds for free: a successful session's visibility/pageshow events always find nothing pending. |
| D-169 | Player-element replacement (no video-ID change) is detected via a new `playerObserver.watchForReplacement(callback)`, registered by `bootstrap.js` once a generation has confirmed its video element. It reuses the *same* single `MutationObserver` `waitForVideo()` already uses — that observer is now created lazily on first use and never disconnected again (re-targeted per generation, `disconnect()` only clears the pending-promise/replacement-watch state) rather than created-and-torn-down per call, since running two `MutationObserver`s concurrently (one for discovery, one for the replacement watch) would violate CLAUDE.md's "exactly one alive at any time" literally | DONE | Claude | Phase 6 | Roadmap 6.1 ("actual media readiness/replacement — not solely on v-parameter change") and 6.3 ("reuse the existing single MutationObserver... never instantiate a second one") together implied this design; there was no third option that satisfies both without keeping one observer alive permanently. A replacement fires `onVideoChange(videoId)` again for the same id, reattaching tracking/resume to the new element. |
| D-170 | Resume's metadata-wait timeout (`establishContentMetadata`'s `metadata-timeout`, D-038's two 5s waits) is deferred, not terminal: `resumeManager.tryResume()` returns the pre-existing (Phase 5, previously unused) `OUTCOME.DEFERRED` and registers a one-shot `loadedmetadata` listener on the video element itself. If metadata later arrives, the listener re-validates eligibility and completes the resume through a newly extracted `completeResume()` helper (the same ad-wait/delay/stabilization/seek/reassert/monitor/UI logic `tryResume()` always ran, now shared) — entirely self-contained inside `resumeManager.js`, no `bootstrap.js` involvement needed | DONE | Claude | Phase 6 | Roadmap 6.2 ("a pending/failed session... preserves its checkpoint and stays eligible for a later readiness event"), R6 specifically. `OUTCOME.DEFERRED` already existed as a reserved-but-unused status from Phase 5 (D-155's note: "reserved for Phase 6's Deferred Recovery Lifecycle") — this is exactly that reservation being spent. `bootstrap.js` needed no new branch for `DEFERRED`: it already falls into the existing non-established path (`protectCheckpoint(saved.time)` + unconditional `arm()`), which is the correct behavior here too (protect the checkpoint until the deferred seek actually lands). Player-discovery timeout (the OTHER "pending/failed" case Roadmap 6.2 names) is handled separately at the bootstrap level (D-168) since, unlike a metadata timeout, nothing was ever set up there for `resumeManager` to resume into. |
| D-171 | `progressTracker`'s 5s save cadence keeps its original tick-count trigger (`ticksSinceSave >= 5`) as the primary condition, with a wall-clock `Date.now() - lastSaveCheckAt >= 5000` check added as an OR-condition alongside it, rather than replacing the count entirely with a pure wall-clock measurement | DONE | Claude | Phase 6 | Roadmap 6.6 ("using elapsed wall-clock time and playback activity rather than assuming a fixed count of interval callbacks represents real elapsed time"). A pure wall-clock replacement was tried first and reverted after it broke ~10 existing harness cases (R7/R9/R21/R22/R24/T3.4/T3-out-of-order/T3-stale-tab/T4-rapid/T5-storage-read-failure/T5-timestamp) that all drive `progressTracker.tick()` manually in synchronous loops of 5 with little or no fake-clock advance between calls — a convention used throughout the suite, not something to rewrite wholesale for one phase. The OR-condition preserves that convention exactly (the count reaches 5 first in every such test, so the wall-clock arm never fires there) while still catching the real risk named: a suspended/throttled background tab where `tick()` itself is called far less often than ~once/second, so relying on the count alone would stretch the effective cadence to many multiples of 5s. Regression-verified: all 46 pre-existing cases plus 3 new Phase 6 cases pass (`node tests/run.js`). |
| D-172 | `progressTracker.stop()` flushes a final sample synchronously as its first action (`flushBeforeTeardown()`), before any state is cleared, reusing the existing `attemptSave(true, 'teardown-flush', true)` path when the video isn't mid-seek. When it IS mid-seek at that exact moment, falls back to a separately-tracked `lastGoodSample` (the most recent settled/non-ad/valid position, refreshed opportunistically on every `tick()` and after `seeked`/`ended`) written directly via `storageManager.saveProgress()` instead of skipping the flush entirely | DONE | Claude | Phase 6 | Roadmap 6.5 ("flush a last-valid, non-ad, settled sample tied to content identity *before* navigation teardown, instead of reading a possibly-repurposed element afterward") and 6.7 ("a pending/in-flight seek is never persisted as a completed playback position"), fixes R10. Reusing `attemptSave` for the common (not-mid-seek) case was chosen over a bespoke flush function to avoid duplicating its ad/live/invalid-position/min-watch guards. A new global `video.seeking` guard was also added to `attemptSave` itself (applies to every trigger, not just teardown) since a save landing exactly mid-seek from ANY trigger — not only teardown — should never be recorded as a completed position. |
| D-173 | `tests/cases/r11-no-recovery-signals-for-restored-tabs.js` rewritten to drive the full pipeline (`loadBootstrap: true`) and assert recovery behaviorally (tracking reattaches and saves after a visibility return), instead of calling `navigationManager.start()` directly and counting `onVideoChange` invocations | DONE | Claude | Phase 6 | The original R11 test asserted a fact about `navigationManager` specifically ("no signal besides a real video-ID change re-triggers `onVideoChange`"), which stays literally true by design (D-168: recovery is intentionally owned by `bootstrap.js`, not `navigationManager` — the latter's job stays narrowly "detect a video-ID change"). Testing the old assertion verbatim would keep it permanently "reproduces" even after the real F02 defect (no recovery path for a restored/frozen tab) is fixed, which would be a false regression signal forever. The rewrite tests the same finding (F02) at the level where the fix actually lives, per CLAUDE.md ("shipped code beats every doc... fix the doc" — the test's over-specific implementation-location assertion was the thing that needed fixing, not the finding). Two more cases added this phase for scenarios the roadmap's VERIFICATION section names but R6/R10/R11 don't cover: `t6-pageshow-restore.js` (bfcache/back-forward restore) and `t6-player-element-replacement.js` (element swap with no video-ID change), plus `t6-spa-exit-pending-seek.js` (D-172's mid-seek fallback path). |
| D-175 | `pendingRecoveryVideoId` (D-168) also gets set when a resume reaches `OUTCOME.PENDING` (seek attempted but never settled within the bounded verify window — e.g. buffering), not only on a player-discovery timeout. A recovery trigger (visibility/pageshow) then re-runs the full pipeline, which re-fetches the still-protected checkpoint and retries the seek. `OUTCOME.DEFERRED` (metadata-wait timeout) is deliberately excluded from this — it already self-heals internally via `resumeManager`'s own `loadedmetadata` listener (D-170), and layering a full pipeline re-run on top would risk a redundant duplicate attempt | DONE | Claude | Phase 6 | Found live during this phase's own Chrome verification (chrome-devtools-mcp, CPU 20x + Slow 3G throttle on a real YouTube watch page): the throttle was severe enough to disrupt seek verification (outcome PENDING) but not severe enough to block player discovery within 10s, so the two originally-implemented recovery categories (D-168/D-170) never covered this session at all. `tests/cases/t5-buffering-several-seconds.js` — written in Phase 5, before Phase 6 existed — already carries the comment "Phase 6 owns resuming this later once buffering actually clears," confirming this was the intended scope of Roadmap 6.2's "a pending/failed session" language all along, not a new decision invented after the fact. New case `tests/cases/t6-pending-seek-recovers-on-visibility.js` added and passes; regression-verified (50 cases total). |
| D-174 | Checkpoint-loss budget (Roadmap 6.8) documented in PRD §5.4 rather than as a testable code guarantee: ordinary teardown (SPA nav, tab close with JS able to run) is bounded by D-172's flush — worst case ~1 poll tick (≤1s) of staleness against the last good sample. Abrupt termination (crash/kill/OS-discard, no JS execution possible) is bounded instead by the last successfully acknowledged `setInterval`/event save — up to 5s under the ordinary cadence, more under a throttled/suspended tab (D-171) | DONE | Claude | Phase 6 | Roadmap 6.8 explicitly frames this as "a documentation deliverable (PRD), not a testable code guarantee" — there is no code path that can distinguish "the OS killed the process" from "the tab is merely suspended" from inside the page, so no test could assert this budget was honored; T6.6 (roadmap) measures ordinary vs. crash/kill/discard separately for exactly this reason rather than claiming one uniform recovery guarantee. |

| D-176 | Live browser verification via `chrome-devtools-mcp` (D-051/D-052 technique) completed for Phase 6/Gate B, with results recorded honestly per what actually executed vs. what the sandbox's real network access couldn't support. **Confirmed live:** (1) ordinary resume is unaffected by Phase 6's changes — a fresh watch page (Despacito, seeded checkpoint at 45s/282s) resumed to a `VERIFIED` outcome with the Restart button shown, landing within a few seconds of the target; (2) the new recovery-trigger wiring genuinely fires in real Chrome — on a real watch page whose resume reached `OUTCOME.PENDING` (seek attempted, never verified — real CDN buffering stall, console showed "Seek could not be verified after 3 attempts"), dispatching a real `visibilitychange` event produced a second, independent resume attempt (a second "Seek could not be verified" warning, from a fresh `attemptId`), proving `pendingRecoveryVideoId`/`attemptRecovery()` (D-168, extended by D-175) actually re-drives the pipeline live, not just in the harness. **Not completed live, and why:** full end-to-end recovery (seek actually landing after the retry) could not be observed — this sandbox's real network access to `googlevideo.com`/ad-tracking domains is unreliable (403s, `ERR_NAME_NOT_RESOLVED`, CORS blocks visible in the console throughout), the same class of limitation D-051/D-060 already documented for this tooling, not a Phase 6 code defect. A genuine bfcache restore (`pageshow` with `event.persisted === true`) could not be forced on real youtube.com in this session — back navigation reloaded the document fresh rather than restoring from bfcache (YouTube's own page likely isn't bfcache-eligible here); the `pageshow` recovery path is exercised directly by the harness (`t6-pageshow-restore.js`) instead. Chrome's own tab freeze/discard (`chrome://discards`) could not be driven at all — `chrome://` navigation is blocked by this tool — so "frozen tab" and "discarded/reloaded tab" were approximated as: a real `visibilitychange` dispatch (frozen→resumed tabs fire this on unfreeze) and a full `navigate_page{type:"reload"}` (what Chrome does when a discarded tab is revisited), respectively, rather than the literal Chrome-internal mechanisms | DONE | Claude | Phase 6 | Roadmap 6's "GATE B CHECK... Record honestly which scenarios were executed and which were not" — this row is that record. Owner asked this session to run the live matrix directly via `chrome-devtools-mcp` rather than being handed a manual checklist; all seeded test storage entries (`dQw4w9WgXcQ`, `M7lc1UVf-VE`, `9bZkp7q19f0`, `yPYZpwSpKmA`, `kJQP7kiw5Fk`, `jNQXAC9IVRw`) were deleted afterward and all extra tabs closed, matching D-167's cleanup precedent. A genuine multi-tab "one active, one inactive, wait 30-120s" restore and a real Chrome-native freeze/discard remain for the owner's own browser session if fuller confidence is wanted — the mechanism itself is proven live (recovery re-fires) and structurally proven end-to-end (harness), but a real successful seek after a real freeze/discard was not directly witnessed this session. |

## v4.0.0 — Phase 7 (Completion Policy & Remove Completed)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-177 | D-116/D-118/D-119/D-120's `Implement In` corrected from "Phase 8 (v4)" to "Phase 7 (v4)" and their Status moved to `DONE`, alongside D-104/D-105/D-106/D-117 (already correctly tagged Phase 7) | DONE | Claude | Phase 7 (v4) | Doc-consistency fix (CLAUDE.md: "a doc is wrong — fix it, log Tier 2"). Those four rows describe behavior inseparable from shipping "Remove completed" itself (its placement, its zero-match state, its confirm-button copy, its per-open checkbox reset) — Roadmap v4's own Phase 7 task 7.5 puts the entire feature in this phase, not split across 7/8; the earlier planning-pass batch (D-116–D-124) evidently defaulted most UX-Spec-planning rows to "Phase 8" without checking each one against the roadmap's actual phase boundaries. D-121/D-123/D-124 were left as Phase 8 — they describe genuinely separate mechanisms (load-failure state already built in Phase 1 per D-131; general row-removal/pin-resort focus-handoff and reduced-motion, neither built this phase). |
| D-178 | Write-side genuine-completion detection: a `'seeked'` event landing within `storageValidation.LEGACY_COMPLETION_TOLERANCE_S` (1s) of `video.duration` sets a session-local `pendingSeekToEnd` flag; a subsequent `'ended'` event only writes `ended: true` if that flag is false, and always consumes (clears) it regardless. Recomputed on every `'seeked'` event so a seek away from the end afterward correctly un-flags it | DONE | Claude | Phase 7 (v4) | D-104 requires the `ended` field to distinguish "a real `ended` event... not a seek-to-end" but does not name a detection mechanism — the DOM `ended` event itself fires identically whether the end was reached by playthrough or by a seek landing at/near the end, so some heuristic was needed. Chosen over alternatives (e.g. tracking cumulative watch time) as the simplest signal that directly targets the one case D-104 calls out: a seek immediately preceding `ended` is the seek-to-end case; anything else reaching `ended` is a genuine finish. The `ended` field itself stays sticky once true (OR-preserved across saves and merges, same as `pinned`) — a later partial rewatch does not retroactively un-complete a video that genuinely finished once. |
| D-179 | `meetsMinimumWatched(savedTime, minWatchSeconds)` changed from strict `>` to inclusive `>=` (Roadmap 7.4) | DONE | Claude | Phase 7 (v4) | Tier 2 value pick resolving the roadmap's own open choice ("make the comparison inclusive... or adjust copy to match strict `>`"). UX Spec CP-42h's existing copy ("less than this") was treated as authoritative over the code, per CLAUDE.md precedence (shipped code beats every doc, but *user-facing copy* is UX Spec's exclusive domain, §7) — rewording an already-shipped, unambiguous setting description to match an arguably-accidental `>` was judged the wrong direction versus fixing the one-line comparison to match its own label. Applies uniformly to every caller (`resumeManager`'s pre-metadata short-circuit and eligibility check, `progressTracker`'s save gate and teardown-flush fallback) since all four already funneled through this one shared function. |
| D-180 | `storageValidation.isCompleteEntry(entry)` is the single completion predicate — used identically by the popup's row display (CP-77 vs. capped percentage), `timeUtils.shouldResume`'s "Only at the end" branch (via a precomputed `isComplete` argument, keeping `timeUtils` free of a `storageValidation` dependency per CLAUDE.md's `utils/`-is-pure-functions convention), and `background/storageWriter.js`'s `handleRemoveCompleted` | DONE | Claude | Phase 7 (v4) | Roadmap 7.7/D-106 requires "Only at the end" to route through "the completion marker from §6.3's Completion Display," i.e. the same predicate, not a re-derived one — a second implementation would risk drifting from D-104's definition over time. `timeUtils` (loaded before `content/resumeManager.js` in manifest order, and conceptually a leaf pure-math module) does not import `storageValidation` itself; instead `resumeManager.js` (which already loads `storageValidation` first, per existing manifest order) computes `isComplete` once and passes it in, keeping `shouldResume`'s own signature side-effect-free and testable in isolation. |
| D-181 | `storageManager.removeCompleted(includePinned)` sends one `REMOVE_COMPLETED` command; `background/storageWriter.js`'s `handleRemoveCompleted` re-derives the matching set from current storage at commit time (never a client-supplied id list) and performs one `chrome.storage.local.get` + one `chrome.storage.local.set`, the same single-read-single-write shape every other writer command uses | DONE | Claude | Phase 7 (v4) | Roadmap 7.5 ("one coordinated batch mutation through the writer") and T7.4/T7.5 (concurrent playback during the action; a simulated storage failure mid-batch must not leave partial state) both require this. Re-deriving server-side instead of accepting a popup-supplied id list closes a TOCTOU gap: the exact set of "currently complete, currently unpinned-unless-opted-in" rows can change between when the popup last read storage and when the user clicks confirm (a concurrent save could complete or un-complete an entry in that window) — the writer's own serialized queue guarantees its read is the freshest available at the moment of the single write. Removed ids each bump `deletionRevisionAt` (7.6), reusing D-143's existing in-memory, worker-scoped anti-resurrection mechanism rather than inventing a parallel one. |
| D-182 | New harness cases this phase: `t7-completion-predicate.js` (isCompleteEntry boundary/fractional-duration/merge-preservation), `t7-only-at-end-sentinel.js` (T7's "Only at the end" resume behavior), `t7-min-watch-boundary.js` (D-179's inclusive boundary, pure function + a real save at the exact preset), `t7-remove-completed.js` (mixed-library batch removal, pinned scope, zero-match, D-181's deletion-revision integration), `t7-remove-completed-storage-failure.js` (T7.5, injected write failure leaves no partial state), `t7-popup-remove-completed-ui.js` (popup harness: completion display capping/labeling, confirm/cancel/commit flow, live count). `tests/lib/harness.js` now also exports `storageValidation`; `tests/lib/popupHarness.js`'s fake DOM extended with the Remove-completed row/controls; `tests/lib/fakeDom.js` gained `closest()` and a no-op `focus()`, both now used by the real `popup.js`. `T2.7`'s expected API surface updated to include the new `removeCompleted` export, same pattern D-141 used for `saveProgress`'s Phase 3 extension | DONE | Claude | Phase 7 (v4) | CLAUDE.md's phase-end instruction to extend the harness per the phase's own test table (T7.1–T7.11) and to fix a failing test (T2.7) rather than leave it red. Full suite (`node tests/run.js`) shows all 56 cases `not-reproduced`/passing, zero regressions. T7.2/T7.3/T7.9/T7.10 are covered within `t7-remove-completed.js`'s single scenario rather than as separate files (the mixed-library seed already exercises zero-match, exact-count, independent-re-derivation-matches, and pinned-exclusion-then-inclusion in one pass); T7.8 (each exact minWatchSeconds boundary) covered at the default (30s) preset only, not all four presets individually — the fix is a single shared comparison with no per-preset branching, so one boundary case exercises the same code path every preset would. |

## Currently blocking

Nothing. D-032/D-033 (privacy policy, store listing) are closed. D-034 (single release vs. split
v1.1 patch) remains open but is optional and non-blocking — v2.0.0 shipped as one release.

Everything else is either approved, done, or Claude Code's to close autonomously.
