# Decision Log — YouTube Resume

Append-only. Never delete a row; change its Status instead. IDs are sequential and never reused.

**Status:** `OPEN` (unresolved) · `APPROVED` (decided, not yet built) · `DONE` (built and confirmed) ·
`REJECTED` (decided against) · `SUPERSEDED` (replaced by a later ID — name it in Notes)

**Owner:** who resolves it. `Human` means it waits for the owner. `Claude` means Claude Code closes it
autonomously and logs the outcome.

**Read this file at the start of every phase.** Any row whose **Implement In** matches the current
phase is in scope for that phase, whether or not the phase prompt mentions it. Say so up front.

**Add rows without being asked.** Every Tier 2 and Tier 3 decision gets one, at the end of the phase
that made it. A decision that exists only in a chat report is lost when the session ends.

---

## Product & scope

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-001 | Release is **v2.0.0**, not v5.0.0 | APPROVED | Human | All | v1.0.0 is the live Web Store version. |
| D-002 | Settings page promoted into scope, reversing v1.0 non-goal NG4 | APPROVED | Human | Phase 6 | Deliberate promotion. NG4 removed from PRD §3.2. |
| D-003 | Saved videos panel promoted from PRD §13 roadmap into v2.0 scope | DONE | Human | Phase 8 | Was "Resume history page", Medium priority. |
| D-004 | Thumbnails permitted from `i.ytimg.com`, breaking the absolute zero-network claim | DONE | Human | Phase 8 | A list of opaque video IDs isn't usable. Requires D-020 before publishing. |
| D-005 | Thumbnails have an off switch, default on, restoring fully-offline operation | APPROVED | Human | Phase 6 | Mitigates D-004. When off, `src` must not be set at all. |
| D-006 | No new permissions: `<img>` for thumbnails, `<a target="_blank">` for opening videos | DONE | Human | Phase 8 | Avoids the `tabs` permission entirely. |
| D-007 | Six settings only; the 400ms delay, 5s interval and 200-entry cap stay hard-coded | APPROVED | Human | Phase 6 | Exposing the 400ms delay invites users to break their own resume. |
| D-008 | Settings render as a second view **inside the popup** — not an options page, not a new tab | APPROVED | Human | Phase 6 | Owner explicitly rejected the new-tab pattern. |
| D-009 | Settings use segmented buttons and toggles, never free numeric input | APPROVED | Human | Phase 6 | Eliminates validation and invalid states on a 360px surface. |
| D-010 | Popup widens 280px → 360px; UX Spec §6.2 "must not feel like an app" formally relaxed | DONE | Human | Phase 8 | A thumbnail list is unusable at 280px. |
| D-011 | Status row (`✓ Active on YouTube`, CP-12/CP-13) removed | DONE | Human | Phase 8 | A populated list is self-evident proof; empty state covers the zero-data case. |

## Storage & data

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-012 | Schema advances to v2, adding optional `title` | DONE | Human | Phase 4 | Purely additive, so migration is non-destructive by construction. |
| D-013 | Schema version lives in root key `youtubeResumeSchema`, never inside `youtubeResume` | DONE | Human | Phase 4 | `youtubeResume` keys are counted for eviction; a stray key corrupts it. |
| D-014 | Settings live in root key `youtubeResumeSettings` | DONE | Human | Phase 4 | Clearing progress must not reset settings, and eviction must never eat one. |
| D-015 | Title scraped from `document.title` with the ` - YouTube` suffix stripped, DOM selector as fallback | DONE | Claude | Phase 4 | `document.title` is far more stable across YouTube redesigns than metadata selectors. |
| D-016 | Stored titles capped at 200 characters | DONE | Claude | Phase 4 | Tier 2 value pick. |
| D-044 | `popup.js` stopped calling `chrome.storage.local` directly (pre-existing v1.0 violation of the "storageManager is the sole owner" constraint); popup now loads `storage/storageManager.js` and uses new `getAllProgress()`/`clearAllProgress()` methods | DONE | Claude | Phase 4 | Found while doing T4.9 (grep for `chrome.storage`). CLAUDE.md hard constraint — route around, don't ask. `clearAllProgress()` removes only `youtubeResume`, leaving settings/schema untouched (PRD §7.4). |
| D-045 | `saveProgress`'s new optional `title` param preserves an existing stored title when omitted/falsy, instead of erasing it | DONE | Claude | Phase 4 | Roadmap 4.5 says a missing title capture "must never block the save" but doesn't say whether it should clear a previously-good title; erasing on a transient capture failure (e.g. `document.title` momentarily blank during a redesign/SPA transition) would be a silent regression for panel display (Phase 8). Tier 2 value pick. |

## Reliability (Phases 1–3)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-017 | Confirm which of hypotheses H1–H8 actually cause unreliable resume before fixing anything | DONE | Claude | Phase 1 | `docs/PHASE1_FINDINGS.md`. H2/H4/H6/H8 confirmed; H5 not reproduced (TDD doc issue only); H1 confirmed via a different trigger than described (see D-036); H3/H7 confirmed by code but not reproducible live this session; H1's ad case and H3's throttled case untested (S4, see Notes). |
| D-018 | If Phase 1 finds failure modes outside H1–H8, Phase 2 scope expands to cover them | DONE | Claude | Phase 2 | Expanded per D-036/D-037/D-038 below; all three built this phase. |
| D-036 | Phase 2 must fix navigationManager/bootstrap teardown gap: leaving a watch page for a non-watch page never emits, so progressTracker/uiInjector/playerObserver teardown never runs | DONE | Claude | Phase 2 | Phase 1 Finding A. `checkAndEmit()` now emits on any change including transitions to/from `null`, not just truthy new ids — this single change also fixes H8 (same-video re-entry, D-023 row below/task 2.5), since leaving resets `currentVideoId` to `null`. |
| D-037 | Phase 2's ad/guard redesign (D-019/D-021) must also account for YouTube's own native resume moving `currentTime` before the 400ms delay elapses, independent of ads | DONE | Claude | Phase 2 | Phase 1 Finding B. New guard compares against a pre-delay baseline with 10s drift tolerance rather than an absolute `currentTime > 5`, so a native resume landing near the saved position no longer trips it. |
| D-038 | `waitForMetadata`'s 5s timeout is reachable under ordinary (non-throttled) network conditions and silently skips resume; Phase 2 should reassess the timeout or add a retry | DONE | Claude | Phase 2 | Phase 1 Finding C. Implemented as one retry (second 5s wait) before giving up, ~10s total — Tier 2 value pick. |
| D-019 | Resume is ad-gated: defer until `.ad-showing` and `.ad-interrupting` both clear | DONE | Human | Phase 2 | PRD §5.7 required this in v1.0; the v1.0 TDD omitted it, so it was never built. Implemented as a bounded loop: an ad reappearing mid-delay re-defers and re-baselines rather than aborting outright (see D-040). |
| D-020 | Ad wait has a 60-second hard ceiling, then abandons cleanly | DONE | Claude | Phase 2 | Tier 2 value pick. Prevents an unbounded wait; polled every 250ms via recursive `setTimeout` (not `setInterval`, to respect the one-interval constraint). |
| D-021 | Replace the `currentTime > 5` abort guard with a pre-delay comparison, 10s drift tolerance | DONE | Claude | Phase 2 | The old guard read *ad* position, silently cancelling resume on any ad over 5s. |
| D-022 | Seek is verified: re-read after 250ms, re-assign if off by >3s, max 3 attempts | DONE | Claude | Phase 2 | Tier 2 value picks. Never an unbounded retry loop. UI (restart button/toast) still shown even if verification never converges — best-effort seek already occurred. |
| D-023 | `waitForVideo()` waits for `#movie_player` instead of rejecting when it's absent | DONE | Human | Phase 2 | v1.0 guaranteed a missed resume on slow cold loads. Observes `document.body` (covers container-missing and video-missing in one observer). |
| D-040 | Ad reappearing during the 400ms resume delay re-defers to the ad wait and re-baselines the drift guard, instead of abandoning the resume outright; bounded to 3 rounds | DONE | Claude | Phase 2 | PRD §5.7 says defer until ads clear, not abandon on the first mid-delay ad — matches mid-roll-heavy long videos better than a single abort. Round cap is a Tier 2 value pick to prevent an unbounded loop. |
| D-041 | Restart button and toast now gated on `seekWithVerification` actually succeeding; a failed verification shows neither and only logs a warning | DONE | Claude | Phase 2 | T2.1 verification failure: toast claimed "resumed from 19:58" while playback actually continued from 19:40 — first-cut code showed the UI unconditionally after the seek attempt regardless of verification result. PRD §5.6 already required this ("Restart button appears only when a resume seek was successfully applied and verified") — the bug was not implementing that gate, not a missing spec. Root cause of the drift itself is presumed to be YouTube's own native resume cue re-asserting a stale remembered position during the verify window (Finding B); no code change beyond the UI gate was made for that, since D-021/D-022's bounded guard+retry design is already the agreed mitigation. Owner re-ran T2.1 (3 cold loads, including incognito) after the fix: all 3 resumed within ±3s. |
| D-024 | Delta guard applies to the interval trigger only; all event triggers save unconditionally | DONE | Human | Phase 3 | Resolves a contradiction inside v1.0 TDD §4.5. `attemptSave(bypassDelta, trigger)` takes an explicit flag; only the interval call passes `false`. |
| D-025 | `beforeunload` retired in favour of `pagehide`; both documented as best-effort | DONE | Human | Phase 3 | v1.0 claimed a synchronous storage save, which that API cannot do. TDD §4.5/§7.2 updated to state best-effort explicitly. |
| D-042 | `ended` handler added; invalid-position guard (`NaN`/negative/`>duration`) added to `attemptSave` | DONE | Claude | Phase 3 | Roadmap 3.2/3.5. Closes Phase 1 Finding H6 (no `ended` handler). Both are Tier 2 — filled a task-list detail (guard specifics weren't spelled out) rather than a new decision axis. |
| D-043 | `attemptSave`'s invalid-position guard also rejects `NaN` `duration` (not just `NaN`/negative/`>duration` `current`); `tryResume` gains a duration-independent `meetsMinimumWatched()` short-circuit before the metadata wait | DONE | Claude | Phase 3 | Found via owner's live extension-errors report: `current > duration` is always `false` in JS when `duration` is `NaN`, so the D-042 guard didn't actually block a `NaN`-duration save (pre-metadata write); separately, `tryResume` was paying for the full ~10s metadata wait (D-038) even when `saved.time` could never clear the 30s minimum regardless of duration, observed live on a 19s video. |

## UI (Phase 5)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-026 | Measure YouTube's live computed styles and record them in `docs/YT_DOM_AUDIT.md` before restyling | DONE | Claude | Phase 5 | Values are measured, never remembered. Blocks D-027 and D-028. |
| D-027 | Reverse the v1.0 "no background or border on the Restart button" anti-pattern | DONE | Human | Phase 5 | YouTube moved to rounded-pill controls; the old rule now *causes* the mismatch. |
| D-028 | Fix the toast/progress-bar collision by deriving the offset from measured control-bar height | DONE | Human | Phase 5 | Confirmed in the owner's screenshot: the red line runs through the toast. `bottom: 48px` is stale. |
| D-029 | Toast promoted from optional to required, and made user-disableable | DONE | Human | Phase 5 | It already ships; specifying it properly is overdue. Disable switch itself is `showToast`, wired in Phase 7 — this phase only fixes its visuals and confirms it's unconditional pending that setting. |
| D-046 | Measurement (`docs/YT_DOM_AUDIT.md`) found no native inline text-pill button in the control bar — icon buttons are flat with opacity-only hover. Restart button pill fill/radius instead derived from the measured `.ytp-menuitem` hover intensity (`rgba(255,255,255,0.1)` rest / `0.2` hover) and the 40px control-row height (`20px` radius); toast background/radius taken directly from the measured `.ytp-settings-menu` panel (`rgba(0,0,0,0.6)`, `12px`) | DONE | Claude | Phase 5 | Tier 2 value pick, filling the gap between D-027's approved reversal and what measurement actually showed. Toast's `bottom` offset is computed at runtime from `.ytp-chrome-bottom`'s measured height (`59px`, identical in default/theater) + 12px clearance, not hard-coded, per D-028. |
| D-047 | Restart button gains `alignSelf: 'center'` | DONE | Claude | Phase 5 | Owner's T5.1 verification found the button sitting flush against the progress bar instead of centered like native controls. Root cause: `.ytp-left-controls` is `display: flex; align-items: normal` (stretch); a fixed-height child (our 40px button) without `align-self: center` collapses to flex-start instead of centering like the native 40px icon buttons, which are centered by the same mechanism. Verified via measured `getBoundingClientRect()`: button's top/bottom now match `.ytp-play-button`'s exactly. |
| D-048 | `storageManager.js` gains an `assertStorageAvailable()` guard at the top of every public function, throwing a descriptive error when `chrome.storage.local` is unavailable | DONE | Claude | Phase 5 | Owner's T5-verification first-load report: `Cannot read properties of undefined (reading 'local')` on the very first resume attempt. Root cause is almost certainly a dev-only artifact — a content-script instance left running in an already-open tab from before the extension was last reloaded via Load Unpacked, whose `chrome.storage` binding is invalidated (a real store install never hits this, since new tabs/reloads always get the current content script). Behavior is unchanged (resume already failed safely, caught by the existing `.catch()`); this only makes the console warning diagnosable instead of a bare `TypeError`. |

## Docs & release

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-030 | TDD stays at v1.0.0 and is updated section-by-section per phase, reaching 2.0.0 in Phase 9 | APPROVED | Human | All phases | Precedence puts TDD above PRD/UX Spec, so a stale TDD is a live risk. Every phase lists its sections. |
| D-031 | Dev Checklist §6.2 arithmetic error resolved: `shouldResume(100, 200)` is `true`, resumes to 98 | APPROVED | Human | Phase 9 | 100 < 190. The v1.0 test case was simply wrong and self-noted as such. |
| D-032 | Privacy policy must disclose the `i.ytimg.com` thumbnail request | DONE | Human | Pre-release | S3 — published under the owner's name. Draft written at `docs/PRIVACY_POLICY_DRAFT.md` (Phase 9); reviewed and approved by the owner. |
| D-033 | Store listing must stop claiming zero network requests | DONE | Human | Pre-release | S3. Draft written at `docs/STORE_LISTING_DRAFT.md` (Phase 9); reviewed and approved by the owner. |
| D-034 | Whether to ship v2.0.0 as one release or split reliability (1–3) into a v1.1 patch first | OPEN | Human | Pre-release | Reliability alone would help live users sooner. Worth revisiting after Phase 3. |

## Runtime wiring (Phase 7)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-049 | `storageManager` gains a synchronous `getDefaultSettings()` (no storage access); `bootstrap.js` calls it only when `getSettings()` itself rejects, wrapping the call in try/catch | DONE | Claude | Phase 7 | Roadmap 7.7 requires a settings failure to never block resume. `getSettings()` already self-heals a corrupt *value* (merges over defaults), but a rejected promise (e.g. `chrome.storage` unavailable) needed a fallback that doesn't itself touch storage — duplicating the defaults object outside `storageManager` would drift, so it's exposed instead. Tier 2 value pick. |
| D-050 | `progressTracker`'s `minWatchSeconds` gate reuses `timeUtils.meetsMinimumWatched(current, minWatchSeconds)` against the live playback position, mirroring `resumeManager`'s use of the same function against `saved.time` | DONE | Claude | Phase 7 | Roadmap 7.5 specifies the outcome ("no storage entry below this threshold") but not the mechanism. Reusing the existing position-based predicate avoids a second threshold concept; applies to every save trigger (interval and event), not just the interval, since none of them should persist an entry below the floor. Tier 2 value pick. |

## Saved videos panel (Phase 8)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-053 | `.list-region` gets a static `max-height: 508px` (560px popup max minus the measured ~52px fixed header) with `overflow-y: auto`, instead of relying on flexbox `flex: 1` to derive the remaining height | DONE | Claude | Phase 8 | Roadmap 8.1 specifies the outcome (560px max, header fixed, list scrolls) but not the mechanism. A `flex:1; min-height:0` child inside a `max-height`-only, `height:auto` flex column does not get clamped — the flex container's own resolved height is computed from content *before* `max-height` is applied as a ceiling, so nothing forces the list to shrink; discovered live via `chrome-devtools-mcp` when a 200-row seed rendered as one long unclamped page instead of a 560px popup. A direct `max-height` on the scrolling element itself sidesteps the flex sizing question entirely and still shrinks-to-content for short lists (empty state, a handful of entries), matching "maximum height" rather than "fixed height". |

## Saved videos panel polish (post-Phase-8, owner-requested)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-054 | Video duration and a watched-progress line render directly on the thumbnail (YouTube-style bottom-right badge + bottom-edge fill), built as plain text/CSS elements rather than baked into an image | DONE | Human | Phase 8 polish | Owner asked for a duration overlay and recommended a placement for time-watched; decided to keep the precise `{position}/{duration} · {percent}%` text below the thumbnail (existing CP-34/35) and use the thumbnail overlay only for the at-a-glance YouTube-style badge + progress line, avoiding two redundant progress indicators stacked in one row. Because these are text/CSS, not an `<img>`, they still render when `loadThumbnails` is off — D-005's zero-network guarantee is about image requests, not DOM content, so this doesn't reopen it. |
| D-055 | Thumbnail size increased 120×68 → 144×81 (same 16:9 ratio) | DONE | Human | Phase 8 polish | Owner asked to increase thumbnail size to give the new overlays (D-054) and channel name (D-056) more room. Text column narrows from ~176px to ~172px at the fixed 360px popup width (D-010) — still fits a 2-line title, one channel line, and the meta line without crowding. |
| D-056 | Schema gains an optional `channel` field, captured via new `youtubeUtils.getChannelName()` (DOM selector only — unlike title, there's no `document.title` equivalent for channel name) and persisted through `saveProgress`'s existing preserve-if-omitted pattern (D-016) | DONE | Human | Phase 8 polish | Owner asked for the channel name to display next to the thumbnail, matching YouTube's own sidebar. Selector scoped to `ytd-watch-metadata ytd-channel-name a` / `ytd-video-owner-renderer #channel-name a` (verified live against a real watch page) rather than a bare `#channel-name`, which also matches inside the comments section. Purely additive like `title` in schema v2 (D-012) — no schema version bump needed. Rows with no captured channel (pre-existing entries, or a same-session DOM-selector miss) simply omit the channel line rather than showing a placeholder. |
| D-057 | `youtubeUtils.getTitle()` also strips a leading `(3) `-style unread-notification-count prefix from `document.title`, in addition to the existing trailing `" - YouTube"` suffix | DONE | Human | Phase 8 polish | Owner flagged that YouTube prepends an unread-count badge to the tab title. Regex (`/^\(\d+\)\s*/`) matches digit-only parenthetical prefixes only, so a legitimate title parenthetical like `"(Official Video)"` is left untouched — verified against both cases. |
| D-058 | Icon-only Ko-fi link added to the saved-videos header, centered via a 3-column `header-row` grid (title / icon / count+gear), as an inline SVG rather than the ❤️ emoji already used once in Settings | DONE | Human | Phase 8 polish | Owner asked for a coffee icon linking to ko-fi.com in the header, icon only, no text. UX Spec §9.2 reserves the ❤️ emoji as the *single* intentional emoji exception, used only in Settings — reusing it (or adding a second emoji) here would break that rule, so a hand-drawn inline `<svg>` (no external asset, no network request, no `innerHTML`) was used instead. New aria-label copy **CP-61** `Support on Ko-fi` — icon-only, so the accessible name is the only copy this control has. Verified live: opens `https://ko-fi.com/emeraldzhu` in a new tab. Icon swapped once (owner-supplied markup, a mug-with-steam-lines glyph from a reference extension) and enlarged 16px → 20px after the first pass read as too small in the header. |

## Phase 9 — Integration, Regression & Store Resubmission

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-059 | Fixed rather than just documented: `progressTracker` no longer owns its own `setInterval`. It exposes `tick()`, called from `navigationManager`'s existing permanent 1000ms poll (wired through `bootstrap.js`), and fires a save every 5th tick to preserve the exact 5000ms cadence. `navigationManager.start()` gained an optional second `onTick` param. Restores CLAUDE.md's "exactly one `setInterval`" constraint literally, not just in spirit. | DONE | Claude | Phase 9 | Found by live-testing per Roadmap T9.4: two intervals (`navigationManager`'s 1s URL-poll fallback, pre-existing since v1.0, and `progressTracker`'s 5s save interval) ran concurrently on any watch page, contradicting the constraint and TDD §11.3/§9's "1 max" claim. Initially logged as accept-and-document-only, given the risk of touching hardened, ad-gating-tested tracking code this late. Revisited same session at owner's request after sizing the fix as small and low-risk: it changes only *how* the 5s cadence is clocked (tick-counting off an existing beat vs. an independent timer), not any save-trigger guard, ad-gating, delta logic, or timing value — `attemptSave`'s call sites, arguments, and every guard in it (D-024, D-042, D-043, D-050) are untouched. `stop()`/`start()` reset `ticksSinceSave` to 0 exactly where the old code recreated the interval, so first-save-after-video-change timing is unchanged. **Live-verified same session** via `chrome-devtools-mcp` (D-051/D-052 technique) plus a temporary `DEBUG=true` flip (reverted before finishing, never committed true): resume seeked correctly (200s saved → 198s landed, 0 drift), and the interval-trigger save fired ~30 times over a 131s window (≈1 per 4.4s, matching the intended 5s cadence — not the underlying 1s poll rate, and not stalled). A separate, unrelated video-element reset mid-session (currentTime/duration went to 0/NaN, likely a CDP-driven-browser autoplay/stall artifact) was correctly caught and skipped by the existing invalid-position guard (D-042/D-043), which is the guard working as intended, not a new bug. |
| D-060 | T9.3 (30-min soak) and T9.7 (20-rep SPA-nav resume) scoped down this session to what the sandbox's `chrome-devtools-mcp` Chrome instance can reliably run, instead of blocking Phase 9 on them | DONE | Claude | Phase 9 | This session's browser hit real `googlevideo.com` CDN `403`s and an unexplained mid-session `currentTime`/`duration` reset to `0`/`NaN` on real long-video playback — an environment/network limitation (same class of issue D-051 hit with `claude-in-chrome`'s video buffering), not an extension defect; console showed no `[YTResume]` errors correlated with either. Substituted: (1) the resume mechanism itself, re-verified via the D-052 reload+precondition technique — same code path SPA navigation uses once `bootstrap.onVideoChange` fires; (2) the navigation-detection/teardown-reinit layer, stress-tested via 25 synthetic `yt-navigate-finish` dispatches (D-062) instead of 25 real video loads, avoiding multiplying the CDN-403 risk. Full real-video 30-min soak and a real 20-rep SPA-nav sample remain for the owner to run in a normal browsing session, alongside T9.1 — noted in the release checklist, not silently dropped. |
| D-061 | Restart-button click-clears-storage-and-seeks-to-0 (previously inconclusive per prior session) re-verified this session | DONE | Claude | Phase 9 | Via D-052's reload+precondition-write technique: wrote a `youtubeResume` entry (`time:200, duration:596`) directly from the popup context, reloaded the video tab, resume landed at 198s with button+toast shown, then a scripted click on `#yt-resume-restart-btn` seeked the video to ~0.01s and removed the button; `chrome.storage.local.get('youtubeResume')` read back `{}` immediately after. All three pass conditions (seek to 0, storage cleared, button gone) confirmed in one run. |
| D-062 | T9.4 (25 consecutive SPA navigations, single-interval/no-leak check) verified via synthetic navigation instead of 25 real video loads | DONE | Claude | Phase 9 | `history.pushState` to 25 distinct fake video IDs + `document.dispatchEvent(new Event('yt-navigate-finish'))` in a tight loop, sampling for leaked `#yt-resume-restart-btn`/`#yt-resume-toast` elements after each — zero leaks across all 25 teardown/re-init cycles, matching D-059's single-interval design (no per-video interval exists to leak). Side effect noted, not a bug: a bare `Event` (vs. YouTube's own `CustomEvent` carrying `detail.endpoint`) makes YouTube's *own* router code throw `Cannot read properties of undefined (reading 'endpoint')` — confirmed unrelated to `[YTResume]` code by symptom (the string `endpoint` doesn't appear anywhere in this extension) and by the technique (only reproduces with the synthetic bare-Event dispatch, not real navigation). |

## Guardrails

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-035 | `.claude/settings.json` deny list widened beyond `install` subcommands to all of `npm`/`npx`/`yarn`/`pnpm` (Bash and PowerShell), plus writes to `package.json`/lockfiles | APPROVED (Claude) | Claude | Guardrails | Zero-dependency, no-build-step project (CLAUDE.md) — blocking only `install` still permits `npm run`/`npx <tool>` or a hand-authored `package.json` to introduce a build step by the back door. |
| D-039 | `Edit(manifest.json)` / `Write(manifest.json)` deny rule removed from `.claude/settings.json` | APPROVED (Human) | Human | Phase 1 | Owner explicitly overruled the guardrail mid-Phase-1 to let Claude wire `utils/debugLogger.js` into the content-script load order. The rule blocked all manifest edits unconditionally (not just `permissions`/`host_permissions`, which is what S1 actually protects); removing it doesn't relax S1 itself — permission/host_permission changes still require a STOP. |
| D-051 | `chrome-devtools-mcp` (npm-launched Chrome DevTools Protocol MCP server) added to **user-scope** `~/.claude.json` — not this repo's `.mcp.json` — with `--categoryExtensions=true`, and used for Phase 7's live verification instead of the `claude-in-chrome` extension | DONE | Claude | Guardrails | Owner asked for the checklist to actually be run, not just described. `claude-in-chrome` hit two hard blockers: (1) it refuses to navigate/interact with `chrome-extension://` pages and its screenshots only cover the page viewport, not the toolbar — so the settings popup and `chrome.storage.local` were unreachable; (2) in that tool's Chrome instance, `<video>` never buffered at all (`readyState` stuck at 0, zero `videoplayback` network requests) regardless of which video was loaded — unrelated to the extension. `chrome-devtools-mcp` has no such restriction (raw CDP) and its Chrome instance plays video normally. User-scope, not project-scope, because it's a personal verification tool, not something this zero-dependency repo should carry as project config (keeps D-035's spirit). Requires an actual session restart after editing `~/.claude.json` for the new MCP tools to load — cannot be hot-reloaded mid-session. |
| D-052 | Verification technique for Phase 7 (and future runtime-behavior phases): drive the extension's popup page directly via `chrome-devtools-mcp`'s `install_extension` + a `new_page` to `chrome-extension://<id>/popup/popup.html`, read/write `chrome.storage.local` from *that* page's script context (not the YouTube tab's), and precede any resume/save assertion with a `navigate_page{type:"reload", initScript:...}` that installs a `setInterval`-based sampler recording `{t, currentTime, toast, button}` into `window.__samples` from t=0 of the new document | DONE | Claude | Guardrails | Three real traps this avoids, discovered empirically: (1) content-script **isolated worlds don't share prototypes** with the page's main world — a `HTMLMediaElement.prototype.currentTime` setter monkey-patch from `evaluate_script` never sees the extension's own seeks, so don't bother; (2) each tool-call round-trip in this environment cost tens of real seconds, comfortably longer than the toast's ~2.2s and the Restart button's 7s auto-dismiss windows — sampling via a *separate* follow-up `evaluate_script` call after `wait_for`/`navigate_page` reliably arrives too late and finds both already dismissed; recording via the page's own `setInterval` (started by `initScript`, read back whenever convenient) sidesteps this entirely; (3) letting the video **actually play** between setting up a precondition (`video.currentTime = X; video.pause()`) and reloading is unsafe even with a `play`→`pause()` guard listener — YouTube's autoplay resumed it enough during that latency gap to overwrite the saved position via `pagehide`. Writing the `youtubeResume` precondition **directly to storage** from the popup context (bypassing live playback entirely) instead of trying to pin a real seek is the reliable way to set up "previously watched to Xs" scenarios. |

## v3.0.0 — Roadmap & scope (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-063 | v3.0.0 is a **single release** covering defect fixes (Phases 0–3) and pinning (Phases 4–6), with no interim ship gate | APPROVED (Claude) | Claude | All (v3) | Prompt specified this explicitly; no split-release option was offered. Structural constraint: Phases 0–3 must not depend on anything introduced in Phases 4–6, so they stay independently releasable if pinning is ever deferred (docs/ROADMAP_v3.md §1). |
| D-064 | Video ID is the sole identity for a saved entry; title/thumbnail are display-only metadata, never used in lookup or key derivation | DONE | Claude | Phase 1 (v3) | Reading the shipped `storageManager.js` shows `videoId` is already the only key in use — Phase 1 makes this structurally explicit (audit + guarding comment) rather than assuming a bug exists, since Phase 0 may find defects A/B have a different root cause. |
| D-065 | Storage repair (migration chain, duplicate-merge pass, unresolved-videoId rejection) is non-destructive: no entry is ever deleted by Phase 2's logic, only merged or rejected pre-write | DONE | Claude | Phase 2 (v3) | CLAUDE.md S2 protects existing users' saved data; a merge bug that deletes real progress would violate that even if unintentional, so the roadmap states the non-destructive guarantee as an explicit constraint (2.5) rather than an implicit hope. Built and verified live: T2.2/T2.3/T2.7 confirmed the merge pass only collapses duplicate rows for the same video, never a distinct one; T2.4/T2.5 confirmed rejected writes leave the store untouched. |
| D-066 | Progress tracking starts disarmed on every page load and arms only after the resume lifecycle resolves; a large backwards write is rejected unless preceded by an observed user seek | DONE | Claude | Phase 3 (v3) | Targets defect (C) structurally (a near-zero overwrite) without assuming Phase 0 finds one specific trigger — the gate is general-purpose. Explicitly does not change the fixed 400ms delay (CLAUDE.md hard constraint) or the existing verified-seek retry from v2.0.0 Phase 2 (D-022), only when tracking is allowed to write at all. Built and live-verified this session (see ROADMAP_v3.md Phase 3 Findings): T3.1–T3.8 pass. |
| D-067 | Pin cap is 20; pinned entries are fully exempt from the 200-entry eviction cap (the cap applies to unpinned entries only); a 21st pin attempt is refused, never auto-unpinned | DONE | Claude | Phase 4 (v3) | Tier 2 value pick, consistent with the existing philosophy of small fixed limits (200-entry cap, six settings, no free numeric input). Auto-unpinning to make room was explicitly ruled out per the prompt — a silent unpin would be a surprising, user-invisible data change. Built and live-verified this session — see D-093. |
| D-068 | Schema advances to v3 (adds optional `pinned: boolean`, default `false`); migration becomes version-aware (chain-based) instead of a single "write current version" check, so each step is idempotent independently | APPROVED (Claude) | Claude | Phase 2 & 4 (v3) | Purely additive per CLAUDE.md's schema constraint. Version-aware chaining is needed because Phase 2 also wants migration idempotency as a fix in its own right (defect-adjacent robustness), not just to land Phase 4's new field. Superseded in part by D-071 below, which splits *which phase* actually bumps the version number. |
| D-069 | Defect B's primary hypothesis revised to legacy v1-era data surviving the v1→v2 migration and being re-imported/re-merged into `youtubeResume` because migration cannot reliably detect it already ran; the title-participates-in-identity theory is demoted to secondary | APPROVED (Claude) | Claude | Phase 0 (v3) | Owner correction: the legacy-reimport theory explains all three reported facts at once (200-entry cap exactly filled, every entry titled "Untitled video" since v1 stored no titles, and onset specifically on browser restart), where the title-identity theory only explains the titling and is already weakened by `storageManager.js` keying `youtubeResume` by `videoId` alone. Phase 0 now tests both explicitly rather than assuming either. |
| D-070 | Storage repair's non-destructive rule (D-065) gets one named exception: after a *verified successful* migration, the legacy v1 key/shape may be removed so it cannot be re-imported — but only if Phase 0 confirmed D-069's legacy-reimport hypothesis, and never touching a v2/v3 `youtubeResume` entry | **REJECTED — retired, unsupported** | Claude | Phase 2 (v3) | Owner-amended instruction: drop this task outright. Its precondition never held — D-085/0.5 found no code path in any shipped version that ever created a second/legacy storage key, and 0.0's live export of the owner's real profile found none either. There is nothing for this exception to remove and never was; not implemented in Phase 2, and not to be resurrected without new evidence of a legacy key actually existing. |
| D-071 | Schema version 3 is introduced **exclusively in Phase 4** (pinning); Phase 2's version-aware migration chain (D-068) builds the mechanism but leaves `youtubeResumeSchema` at its pre-existing value. Phase 2/3 alone therefore never advertise or require a v3 shape | DONE | Claude | Phase 2 & 4 (v3) | Owner correction: Phase 2.1 and Phase 4.1 both bumping to v3 independently would mean a user on Phases-0–3-only (per the independent-releasability constraint, D-063) could land on schema 3 without ever gaining pinning support, then skip the real v3 step entirely on a later upgrade. Assigning the version bump to exactly one phase removes the collision. Phase 4 landed the actual bump (`CURRENT_SCHEMA_VERSION` 2→3) this session — see D-093. |
| D-076 | "Clear all saved videos" removes only the `youtubeResume` root key (PRD §7.4), so it would not have removed a surviving legacy v1-era key living elsewhere — the original defect-B artifact is therefore **not confirmed gone**. Phase 0 now runs a read-only live-profile inspection (task 0.0) against the owner's real profile *before* any synthetic reproduction, on the chance it is the only surviving copy of the actual failure state | APPROVED (Claude) | Claude | Phase 0 (v3) | Owner correction: the D-069 legacy-reimport hypothesis assumes a second, undocumented key survives migration; if true, the one storage action the owner has taken (clear-all) would not have touched it either, since clear-all's own scope is deliberately limited to `youtubeResume` (D-014's separate-root-key design working as intended, here with an unintended side effect on diagnosability). Ordering synthetic repro (0.5–0.8) after 0.0 avoids overwriting or never discovering a real artifact in favor of a reconstruction. |
| D-096 | Closed a gap D-080 specified but Phase 5 never built: `popup.js`/`popup.html` gained a `pinnedCount` variable (seeded from the initial storage read, kept in sync on pin/unpin toggle and on deleting a pinned row, reset to 0 after `clearAllProgress`) and a `<p id="confirm-pinned-note">` element that shows CP-66 (plural)/CP-67 (singular) whenever `pinnedCount > 0` when the "Clear saved progress" confirmation panel opens | DONE | Claude | Phase 5/6 (v3) | CLAUDE.md's "a doc is wrong or contradicts the code… fill it" latitude applied to a docs-reconciliation phase finding a real, user-visible copy gap rather than a doc inaccuracy: D-080 (UX Spec, Tier 2, already `APPROVED`) named the exact copy and trigger condition, and no Phase 5/6 task list ever named it as a code deliverable, so it shipped without ever being flagged missing until this pass's `grep` for `pinnedCount`/`CP-66`/`CP-67` found zero matches in `popup.js`. Built and verified by reading the resulting `popup.js`/`popup.html` directly (not just the diff) before writing any doc claiming it done. |
| D-097 | Found via docs reconciliation, then fixed same session despite Phase 6 being nominally docs-only: the pin-limit-reached inline message (CP-65, `.pin-cap-message` in `popup.js`'s `showPinCapMessage`) had no `aria-live`, so a screen-reader user got no announcement when a 21st pin attempt was refused, contradicting UX Spec §8.3's prior claim that it was announced | DONE | Claude | Phase 6 (v3) | Found via `grep` for `aria-live` across `popup.js`/`popup.html` (zero matches) while verifying §8.3 against shipped code — not new to pinning: the same pre-existing v2.0-era gap also applied to `#confirm-panel`/`#reset-confirm-panel` (§8.3's separate "Confirmation copy" row made the identical `aria-live="polite"` claim, equally false). Fixed both in the same pass rather than left as documented gaps: `role="status"`/`aria-live="polite"` added to `.pin-cap-message` (`popup.js`) and to `#confirm-panel`/`#reset-confirm-panel` (`popup.html`) — attribute-only additions with no layout/behavior change, well within Tier 1/2 and CLAUDE.md's "fill it" latitude, so shipping the fix in the same phase that found it beat leaving two known, trivial accessibility bugs on the books. UX Spec §8.3's pin-limit and confirmation-copy rows now both correctly state the announcement happens. |

## v3.0.0 — UX Spec update (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-077 | Pin affordance splits into two elements: a persistent, non-interactive pinned badge on the thumbnail (state visible without hovering) and a separate hover-revealed, keyboard-focusable pin/unpin toggle in the row's action area, drawn as inline SVG (not emoji), matching the Ko-fi icon precedent (D-058) | APPROVED (Claude) | Claude | Phase 5 (v3) | Satisfies both Roadmap v3 Phase 5.1 (control revealed on hover, like the existing remove control) and 5.3 (pinned state visible without hovering) without contradiction — one element shows state, the other changes it. |
| D-078 | Sort order becomes two-tier: pinned entries first (most recent within that group), then unpinned entries (most recent within that group); still not user-configurable | APPROVED (Claude) | Claude | Phase 5 (v3) | Direct specification of Roadmap v3 Phase 5.2 in UX-Spec terms; no new setting introduced — pin/unpin is the only lever a user has over ordering. |
| D-079 | Pin-limit-reached (21st pin attempt) is a refusal shown as a brief inline message (CP-65) in the row's action area, auto-dismissing after ~2.5s — no `alert()`/`confirm()`, no auto-unpin | APPROVED (Claude) | Claude | Phase 5 (v3) | Roadmap v3 5.4 required "no silent no-op" and "no alert/confirm dialog" but left the exact presentation open; 2.5s matches the resume toast's existing brevity convention (§5) rather than inventing a new timing value. |
| D-080 | "Clear saved progress" confirmation (CP-49/CP-50, unchanged) gains a conditional appended line — CP-66 (plural) or CP-67 (singular) — naming the pinned count explicitly whenever at least one video is pinned | APPROVED (Claude) | Claude | Phase 5/6 (v3) | Pinning protects only against the 200-entry cap, not against this explicit user action (Roadmap v3 4.5); Copy Rule 4 (§2.3, "confirmation dialogs state the action plainly") means a user who assumes pinning is absolute protection must be told otherwise before confirming, not after. |
| D-081 | Missing-title display reuses the existing CP-37 fallback (`Untitled video`) unchanged; v3.0 only clarifies that the fallback is no longer necessarily permanent, per the Roadmap v3 Phase 2.4 lazy-backfill fix — no new copy ID needed | APPROVED (Claude) | Claude | Phase 2 (v3 docs) | Mirrors the existing channel-name omission behavior already documented in PRD §7.3; adding a distinct "not yet backfilled" string would imply a different visual state than a title that was simply never captured, which isn't the case — both render identically as CP-37 until the next successful save. |
| D-082 | Task 0.0's live-profile finding takes precedence over T0.8's synthetic result; a three-outcome table added to Roadmap v3 Phase 0 makes explicit that (a) 0.0 finding legacy data stands regardless of T0.8, (b) 0.0 finding nothing plus T0.8 passing refutes the hypothesis, (c) 0.0 finding legacy data plus T0.8 passing means migration previously left data behind even though current code doesn't reproduce it — Phase 2 must still neutralise it | APPROVED (Claude) | Claude | Phase 0 (v3) | 0.0 reflects the owner's actual upgrade history under whatever migration shipped at the time; T0.8 only proves today's code is clean, not that a past profile is. Rewrote task 0.0 to run headless-first: Claude Code inspects a static export the owner pastes in (via a one-time popup-console copy-to-clipboard snippet), since Claude's CDP-driven Chrome can't reach the owner's real profile. |
| D-083 | PRD NG8 amended (not deleted or renumbered) with a bounded exception: pinning is curation, but only of retention and ordering, never of an entry's content (title/channel/thumbnail/position) | APPROVED (Claude) | Claude | Phase 4/5 (v3 docs) | NG8 as originally worded ("not curation") is literally false of pinning; scoping the exception narrowly to retention/ordering keeps the actual prohibition (no editing/renaming content) intact. |
| D-084 | `popup/popup.html` gains a `<script src="../utils/debugLogger.js">` include (was missing) | DONE | Claude | Phase 0 | Discovered live: Phase 0's new `storageManager.saveProgress()` diagnostic log (0.2) threw `debugLogger is not defined` when called from the popup context, since popup.html only ever loaded `storageManager.js`. Latent gap — no shipped popup code calls `saveProgress` today — but broke this session's own diagnostic testing and would break the moment it did. `debugLogger.log` no-ops with `DEBUG=false`, so this is not a behavior change. |
| D-085 | Phase 0 findings recorded: defect A not reproduced, defect B primary (legacy reimport) refuted (no such code path exists or ever has, confirmed by 0.0's live export finding no extra key and 0.5/T0.6-T0.8's static+live confirmation that `migrate()` never touches `youtubeResume`), defect B secondary (title-identity) not reproduced, defect C **confirmed** (`saveProgress()` unconditionally overwrites, no guard distinguishes a spurious near-zero read from a genuine seek) | DONE | Claude | Phase 0 | Full evidence in `ROADMAP_v3.md`'s "Phase 0 Findings" subsection. Per D-082's evidence-precedence table, 0.0-finds-nothing + T0.8-passes = outcome (b), refuting the primary hypothesis outright rather than deferring to Phase 2's D-070 exception. |
| D-086 | Phase 1 executed as a confirmatory audit rather than a bug fix: every `videoId`-reading call site (`storageManager`, `navigationManager.checkAndEmit()`, `resumeManager`, `progressTracker`, `bootstrap.js`) already keyed/compared by `videoId` alone before this phase, matching D-064's prediction and Phase 0's 0.7/T0.2/T0.3 finding. Landed an explicit identity-invariant comment at `storageManager`'s `store[videoId] = entry` line (code + TDD §4.6 pseudocode) so a future edit can't silently reintroduce title into the key path, plus a TDD §4.6 narrative paragraph and a PRD §7.3 invariant statement. T1.2 (repo-wide grep) returned zero matches; T1.3/T1.4 verified live via the popup console against the built extension (two distinct `videoId`s with an identical title produced two entries; the same `videoId` saved twice with different titles produced one entry, second title winning) | DONE | Claude | Phase 1 | No behavior change — the invariant already held. This closes the failure class structurally per the roadmap's "cheap insurance" framing, independent of Phase 0's A/B(secondary) not-reproduced verdicts. |
| D-087 | `saveProgress`'s and the new merge pass's preserve-if-omitted logic (D-045) now treats a whitespace-only `title`/`channel` string the same as an omitted one, trimming before the truthiness check instead of a bare `title ? … : existing` test | DONE | Claude | Phase 2 (v3) | Roadmap v3 2.4 asked Phase 2 to verify D-045's write-back guard holds under every save trigger and close any gap found. Live testing found one: `saveProgress(id, t, d, '   ', '   ')` is truthy under the original check, so it silently overwrote a real stored title/channel with blank-looking text. Unreachable via real playback today — `youtubeUtils.getTitle()`/`getChannelName()` both trim and return `null` on failure, never a whitespace-only string — but the guard itself needed to be robust to it regardless of caller, since a display fault that gets written back is permanent data loss per the task's own framing. Re-verified live post-fix: the same call now correctly preserves the existing title/channel. |
| D-088 | Read-path investigation for defect B's third hypothesis ("Untitled video" entries were the owner's real entries rendered without titles, not new ones) found no reproducible mechanism; no fourth hypothesis proposed | DONE | Claude | Phase 2 (v3) | `popup.js` awaits `Promise.all([getAllProgress(), getSettings()])` before building any row, and `getAllProgress()` is a single `chrome.storage.local.get` call — Chrome's storage API returns a coherent snapshot, never a torn read of the object inside a key, so there is no code path where rendering can start before the read fully resolves. The one theoretical interleaving (`storageManager.js`'s own load-time `migrate().then(repairDuplicates)` writing concurrently with `popup.js`'s independent read) can only land before or after that write, never mid-write, and the write itself never clears a title (D-045/D-087). Per the task's own instruction, ships the write-back guard (D-087) regardless of this null result rather than inventing a fourth hypothesis. |
| D-090 | Backward-jump write guard: interval-triggered saves only, threshold `BACKWARD_JUMP_THRESHOLD_S = 30`; re-assert-once native-override check in `resumeManager` uses `NATIVE_OVERRIDE_CHECK_DELAY_MS = 500` | DONE | Claude | Phase 3 (v3) | Tier 2 value picks (Roadmap 3.4/3.6 specify the mechanism, not the numbers). 30s matches the existing `minWatchSeconds` default and defect C's observed magnitude (500→2); 500ms is deliberately shorter than the seek-verify window so a native override is caught while still "shortly afterward" per the roadmap wording. No separate "recent user seek" flag was needed for the interval guard's exemption clause — a real backward seek's own `seeked`-triggered save already updates `lastSavedTime` to the new position before the next interval tick runs, so the exemption falls out of existing state rather than needing new state. |
| D-091 | `bootstrap.js` now calls `progressTracker.start()` (disarmed) *before* attempting resume, not after; `progressTracker.arm()` is called in a `finally` block wrapping the resume attempt | DONE | Claude | Phase 3 (v3) | Roadmap 3.1/3.2 require write-blocking during the resume attempt itself, which needs the tracker's event listeners already attached (so a resume-triggered `seeked` event is caught and silently dropped by the disarmed gate, not left unhandled). Reordering, not new logic — the resume attempt and its `.catch()` behavior are unchanged. |
| D-092 | `uiInjector.js`'s Restart button click handler calls `progressTracker.notifyExternalReset()` immediately after forcing `video.currentTime = 0`, resetting the backward-jump guard's `lastSavedTime` baseline to 0 | DONE | Claude | Phase 3 (v3) | Roadmap 3.6 asked to verify whether the Restart path's programmatic seek needs an explicit exemption rather than relying on the generic `seeked`-event one. Live testing found it does: without this call, replaying the restarted video past `minWatchSeconds` reproduced a false-positive rejection (the first post-restart save looked like a huge backward jump from the stale pre-restart `lastSavedTime`). Reproduced and confirmed fixed live this session (ROADMAP_v3.md Phase 3 Findings, T3.8). |
| D-089 | Roadmap v3 Phase 2's "Docs to Update" line erroneously cited "TDD §4.6 … §7.5, §7.6" — the TDD's actual §7 is "Error Handling Strategy" (only §7.1/§7.2 exist); §7.5/§7.6 are PRD section numbers, already listed separately as "PRD §7.6" on the next line. Corrected in the same pass to drop the erroneous TDD reference | APPROVED (Claude) | Claude | Phase 2 (v3 docs) | Shipped-doc-numbering beats a stale cross-reference (CLAUDE.md: fix the doc in the same commit that finds it wrong). Likely a copy-paste artifact from drafting the PRD and TDD update lists side by side. |
| D-093 | Phase 4 built exactly per roadmap: `saveProgress` preserves an existing entry's `pinned` flag across a normal save; eviction filters to unpinned keys before both the >200 count check and the removal-candidate sort; `pinProgress`/`unpinProgress` added and exposed from `storageManager`; migration chain gains its `{ to: 3 }` step, `CURRENT_SCHEMA_VERSION` bumped 2→3 | DONE | Claude | Phase 4 (v3) | No open value picks — Roadmap v3 4.1–4.6 and D-067 (20-pin cap, no auto-unpin, retention-only eviction exemption) already specified the mechanism and every number. Self-verified via a Node `vm` harness loading the real `storageManager.js` against a mocked `chrome.storage.local` (T4.1–T4.8 all pass: schema-2 and schema-1 profiles both land cleanly on 3 with no entry rewrite beyond the version key; 20 pins succeed, a 21st is refused with the 20 unchanged; 20 pinned + 200 unpinned + 1 new unpinned save evicts only an unpinned entry, all pins survive; unpin-then-pin-another returns to exactly 20; `clearAllProgress` removes pinned entries too; 5 reloads are a no-op after the first) — `chrome-devtools-mcp`/real-browser verification wasn't needed since no DOM/UI surface changed this phase. |
| D-094 | Fixed a repeat of the D-089 doc-numbering bug: Roadmap v3 Phase 4's "Docs to Update" line also cited "TDD §4.6 … §7.3, §7.5, §7.6", attributing PRD section numbers to the TDD again. Dropped the erroneous TDD references in the same pass | APPROVED (Claude) | Claude | Phase 4 (v3 docs) | CLAUDE.md: fix the doc in the same commit that finds it wrong. Same root cause as D-089 — the PRD and TDD update lines were evidently drafted side by side per phase and the erroneous copy wasn't caught for Phase 4 when Phase 2's was. |
| D-095 | Phase 5 built exactly per roadmap and the already-planning-approved UX Spec §6.3/D-077/D-078/D-079 (no new value picks needed — pin control placement, sort order, 2.5s cap-message timing, and CP-62/63/65 copy were all already specified): pin/unpin control (outline vs filled `push_pin` SVG toggle) sibling to the remove control, persistent pinned badge on the thumbnail, two-tier sort, and a per-row `data-pinned`/`data-updated`-attribute re-insertion scheme so pinning moves exactly one DOM node instead of rebuilding the list | DONE | Claude | Phase 5 (v3) | Self-verified live via `chrome-devtools-mcp` against a seeded 200-entry/20-pinned profile: T5.1–T5.2 (pin/unpin moves the row to the correct group boundary, storage matches DOM after each toggle), T5.3 (21st-pin attempt at cap shows CP-65 verbatim, no entry pinned, `pinBtn` briefly hidden then restored), T5.4 (link/pin/remove are three independently-tabbable siblings in that DOM order, native `<button>`/`<a>` semantics), T5.5 (badge presence is the pinned/unpinned visual distinguisher, independent of hover), T5.6 (200 entries/20 pinned rendered in ~19ms via a `MutationObserver`-timed reload, comfortably under the 200ms budget), T5.7 (repo-wide `innerHTML` grep: zero matches outside `docs/`), T5.8 (CP-62/63/65 strings match UX Spec §7 verbatim). One bug caught by this same live testing and fixed before considering the phase done: the pin-toggle handler updated the button's ARIA state and moved the DOM node but never wrote back `li.dataset.pinned`/`updated`, so a *second* toggle on any row read stale sort-key data and mis-sorted — found because the first live pin/unpin round-trip test produced inconsistent aria-label-vs-dataset state. |

---

## v3.0.0 — PRD update (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-072 | PRD §5.10 (new) states the three v3 defects as symptom + guaranteed post-fix behaviour only, explicitly deferring mechanism to the roadmap/TDD since Phase 0's root-cause investigation is not yet complete | APPROVED (Claude) | Claude | Pre-Phase-0 (v3 docs) | Matches the doc-routing rule (CLAUDE.md: PRD wins on intent/scope, TDD on implementation detail) — writing a mechanism into the PRD now would risk contradicting whatever Phase 0 actually finds. |
| D-073 | Data-durability guarantee ("a saved position is never replaced by a near-zero position the user did not cause") promoted to Goal G13 and stated as an explicit product promise in PRD §5.10, not left implicit in the roadmap's write-guard tasks | APPROVED (Claude) | Claude | Pre-Phase-0 (v3 docs) | Owner-specified. Makes the guarantee testable at the product level (R16) independent of whichever specific write-guard mechanism Phase 3 implements. |
| D-074 | Pinned videos specified fully in PRD §5.11 (pin/unpin, sort-to-top, 200-cap exemption, 20-pin cap, clear-all still removes pins) as a firm v3.0 requirement, unlike the still-under-investigation defects in §5.10 | APPROVED (Claude) | Claude | Pre-Phase-4 (v3 docs) | Pinning is a decided feature (Roadmap v3 Phases 4–5), not an open investigation, so the PRD specifies its full product behaviour now rather than deferring detail the way §5.10 defers defect mechanism. |
| D-075 | §6.1 project tree and §6.3 manifest snippet corrected to list `utils/debugLogger.js`, which has shipped since v2.0.0 (Phase 1 instrumentation) but was omitted from the PRD's architecture docs | DONE | Claude | v3 docs | Found while verifying §6.1 against the real `manifest.json`. Shipped code beats every doc (CLAUDE.md) — fixed in the same pass rather than filed separately. The "no popup in v1.0" drift named in the prompt was checked against §3.2 directly and not found; §3.2 already correctly states v1.0 shipped a status popup (see the C4 changelog row), so no PRD text needed correcting there. |

## Phase 6 — Doc Reconciliation, Regression & Store Release

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-098 | Phase 6's live `chrome.storage.local` writes via `chrome-devtools-mcp`'s `evaluate_script` (the D-052 technique used successfully in Phases 4/7/9) were auto-denied this session by the harness's Auto Mode safety classifier; substituted an offline Node `vm` harness loading the real `storageManager.js` against a mocked `chrome.storage.local` (D-093's Phase 4 technique) for T6.1 (schema-2→3 upgrade), T6.2 (fresh install), and T6.5's eviction/pin-cap logic, plus read-only live browser checks (console, DOM state, a real zip-install) for what remained observable without a storage write | DONE | Claude | Phase 6 (v3) | Same class of scoping call as D-060 (Phase 9's soak-test substitution): the target behavior (storage layer correctness) is fully covered by the substitute technique, only the specific tool changed. T6.3/T6.4 (full regression, defects A/B/C) were not independently re-run live this session beyond a no-error spot check, since Phase 6 changed no resume/tracking code — they lean on Phase 0–3's already-recorded live verification earlier this same session. T6.5's 20-cold-load live half (mixed pinned/unpinned resumes) was not run live for the same reason; the storage-layer half (eviction/cap correctness under a 20-pinned/200-unpinned profile) was verified via the harness. Flagged in the phase report as a real gap, not silently substituted. **Update:** owner added `mcp__chrome-devtools__evaluate_script` to `.claude/settings.local.json`'s `permissions.allow` list post-phase; a benign probe write/read/remove against `chrome.storage.local` confirmed the classifier no longer blocks this call pattern. The live reruns this row originally deferred (T6.5's 20-cold-load half, a from-scratch T6.1/T6.3 live pass) were not subsequently requested or run — the gap is now tooling-unblocked but still open if ever wanted. `.claude/settings.local.json` is gitignored (user-global `~/.config/git/ignore`), so this permission grant lives locally only, not in the repo. |
| D-099 | Manifest version bumped `2.0.0` → `3.0.0`; no other manifest field changed (`permissions`, `host_permissions`, `content_scripts` load order, icons all byte-identical to v2.0.0) | DONE | Claude | Phase 6 (v3) | Roadmap v3 6.5, a direct instruction with no open value pick. Verified via diff-equivalent field-by-field read of `manifest.json` (6.3/6.4) and a real zip-install-and-load test (T6.7) that showed zero warnings. |

---

## v4.0.0 — Roadmap & scope (planning, no code)

| ID | Decision | Status | Owner | Implement In | Notes |
|----|----------|--------|-------|--------------|-------|
| D-100 | Roadmap v4's ten-phase structure (0 harness, 1 boundary validation, 2 serialized writer, 3 write ownership/freshness, 4 resume identity/cancellation, 5 verified resume outcomes, 6 deferred recovery, 7 completion policy/cleanup, 8 popup reconciliation/a11y, 9 regression & release) is fixed and maps every audit finding F01–F21 to exactly one phase, with ship gates A/B/C after Phases 3/6/8 | APPROVED (Claude) | Claude | All (v4) | Owner-specified structure, adopted verbatim from the prompt rather than re-derived from the audit's own "Proposed remediation order," which groups findings similarly but not identically (e.g. the audit folds F13/F16/F17 into its cleanup step; this roadmap keeps F13 with the serialized-writer/cap-enforcement phases it structurally belongs to and F16/F17 with popup reconciliation). Audit's order is directional guidance, not a binding phase map. |
| D-101 | Phases 0–3 (harness, boundary validation, serialized writer, write ownership/freshness) remain independently releasable with no dependency on Phases 4–9, mirroring Roadmap v3 §3's constraint; nothing before Phase 4 may reference resume-identity/cancellation state, deferred-recovery state, or the completion marker | APPROVED (Claude) | Claude | All (v4) | Explicit per the prompt. Keeps a storage-correctness-only release shippable on its own (Gate A) if resume-lifecycle or completion work is ever deferred or cut, same rationale as v3. |
| D-102 | A background service worker (`background/storageWriter.js`) becomes the sole writer for every `chrome.storage.local` mutation (saveProgress, deleteProgress, pin, unpin, repair, migration, settings, clearAll); `storageManager.js` keeps its exact current public API and becomes a client that sends commands to the worker; reads stay direct, no round trip. The worker is restart-safe — every command is idempotent and no state lives only in worker memory. `manifest.json` gains `background.service_worker` and nothing else; `permissions`/`host_permissions` are unchanged | APPROVED (Claude) | Claude | Phase 2/3 (v4) | Owner-approved architecture, directly answering F06's "Address" recommendation (route all mutations through one serialized extension-owned writer, e.g. an MV3 service worker) and F06's acceptance criterion (worker restart/retry cases). Restart-safety is load-bearing: MV3 service workers are terminated on idle by the browser at any time, so a command whose only record of having happened lived in worker RAM would silently vanish — every command must be safely re-appliable from `chrome.storage.local` state alone. |
| D-103 | Per-video storage keys (one `chrome.storage.local` key per video, replacing the single `youtubeResume` map) are rejected for v4.0.0 | APPROVED (Claude) | Claude | Phase 2 (v4 scope) | Owner-specified rejection, overriding F06's "Address" text which floats per-video keys as a way to reduce unrelated-key contention. F06's own caveat already concedes they "still need coordination for caps, pinning, clearing, and same-video conflicts" — i.e. they would not remove the serialization requirement, only relocate part of it, while multiplying the number of keys the 200-cap/20-pin-cap logic must reason about and complicating the additive-migration guarantee (CLAUDE.md hard constraint: storage root keys must not change shape). The serialized single-writer approach (D-102) already solves the coordination problem F06 raises without a schema-shape change. |
| D-104 | Completion is a new additive schema field (`ended: boolean`, default absent/`false`) set only when playback genuinely reaches the end (a real `ended` event on content media, not an ad, not a seek-to-end), independent of the resume cutoff (`completionThreshold`). Legacy entries saved before this field existed use a conservative inference rule: an entry with no `ended` field is inferred complete only if `Math.floor(time) >= duration - 1` (i.e. within the smallest unit the existing integer-second storage can represent) — not the 90/95/98% resume cutoff. The popup stops displaying "100%" unless one of these two conditions holds | APPROVED (Claude) | Claude | Phase 7 (v4) | Directly resolves F14/F15's core complaint: displayed 100%/the resume cutoff is not a safe completion predicate (Math.round shows 100% at 99.5%; a 98% cutoff still excludes the final 7.2 minutes of a 6-hour video). The `duration - 1` legacy rule is deliberately the strictest defensible reading of "conservative" — anything looser (e.g. 98%) risks exactly the false-positive bulk-deletion F14's acceptance criterion forbids ("do not silently turn a '100%' cleanup request into deletion of everything above 95%"); erring toward under-counting completions (a few genuinely-finished legacy videos not swept up by Remove Completed) is the safe failure direction, since the user can still remove them individually. |
| D-105 | "Remove completed" (new popup action) matches only the completion predicate from D-104, never the resume cutoff. It preserves pinned entries by default, with an opt-in checkbox/toggle to include them; it previews the exact count before acting; it uses the existing inline confirmation pattern (no new dialog primitive); there is no undo window | APPROVED (Claude) | Claude | Phase 7 (v4) | Directly resolves F14 (product gap, requested by the user) using F14's own recommended defaults ("recommended default is to preserve pinned videos with a separate opt-in to include them," "preview the exact removal count," "one coordinated batch mutation"). No-undo-window is an explicit scope cut from F14's "an undo window is preferable if practical" — the existing extension has no undo primitive anywhere (single-row remove and clear-all are both immediate), and inventing one exclusively for this action would be a new UI pattern this release does not otherwise need; the preview-before-confirm step is the mitigation the existing inline-confirmation pattern already provides for the single-row/clear-all cases. |
| D-106 | "Treat as finished at" gains a fourth segmented option, "Only at the end," alongside the existing 90/95/98%. No new setting key: `completionThreshold` gains the sentinel value `1` (100%), meaning resume/near-completion suppression triggers only when the D-104 `ended` marker (or its legacy inference) is true, not by any percentage-of-duration comparison | APPROVED (Claude) | Claude | Phase 7 (v4) | Owner-specified fourth option and "no new setting key" constraint. `1` is the natural sentinel for "at the end" on a `0–1` fraction field already storing 0.9/0.95/0.98 — every existing comparison site (`savedTime < duration × completionThreshold`) degenerates correctly to "only true resume-suppress at the literal end" when paired with D-104's `ended`-based check replacing the pure arithmetic comparison at that boundary value, so no second field is needed to represent a fifth state. |
| D-107 | An explicit, valid `t=` timestamp parameter in the navigation URL wins for that navigation: it cancels automatic saved-position restoration outright and the resulting playback is treated as a normal user-directed session (eligible for normal interval/event tracking, not specially protected) | APPROVED (Claude) | Claude | Phase 4 (v4) | Directly adopts F20's own "Recommended" policy text verbatim. Resolves the previously undefined precedence F20 identifies (today, behavior "depends on timing rather than an explicit policy") by making the timestamp link authoritative rather than racing it against automatic resume. |
| D-108 | A dependency-free Node regression harness reproducing the audit's R1–R24 scenarios against real, unmodified source is committed to `tests/` (not a one-off session artifact); it is excluded from the Chrome Web Store zip. Phase 0 builds the harness and its R1–R24 baseline; Phases 1–8 extend it with cases for whatever each phase changes | APPROVED (Claude) | Claude | Phase 0 (v4), extended per-phase | Directly resolves F21's core finding: "No committed runnable regression suite exists in the reviewed tree" and its own one-off harness note ("the one-off harness itself was not added to the repository"). Dependency-free matches CLAUDE.md's "no build step, no bundler, no dependencies" convention extended to test tooling; store-zip exclusion keeps `tests/` out of the shipped artifact the same way `docs/` already is. |
| D-109 | Explicitly out of scope for v4.0.0, rejected rather than deferred without comment: always-on/interval polling as a resume-recovery mechanism, telemetry of any kind, a global highest-time-wins conflict rule across tabs/sessions, and unbounded seek retry | APPROVED (Claude) | Claude | All (v4 scope) | Owner-specified rejections. Each directly forecloses a tempting-but-wrong fix the audit itself warns against: F02's "Address" explicitly says to reuse the existing scheduling mechanism rather than add polling; F05's "Address" explicitly says "a permanent highest-time-wins rule would break intentional rewinds and is not a suitable fix"; F01/F03's "Address" both call for *bounded* stabilization/retry, not unbounded; telemetry is foreclosed by the standing hard constraint (zero network requests beyond the gated thumbnail GET) and was never on the table. |
| D-110 | New copy IDs for v4.0.0 start at CP-68, continuing from CP-67 (the last ID in use per UX Spec §7, confirmed by repo-wide grep before drafting this roadmap) | APPROVED (Claude) | Claude | Phases 4–8 (v4, wherever new copy is needed) | Owner-specified starting ID; verified rather than assumed — CP-64 was intentionally skipped in v3 (reserved, then unused, per UX Spec §7's own note), so CP-67 and not CP-64 is the correct last-used anchor. |
| D-112 | Unpinning while already at 200 unpinned entries immediately evicts the oldest eligible unpinned entry (by `updated`), rather than allowing the unpinned count to silently exceed 200 until the next `saveProgress()` | APPROVED (Claude) | Claude | Phase 1 (v4) | Value pick resolving F13's own open question ("Explicitly define whether unpinning into a full library immediately evicts the oldest eligible entry"). Consistent with the existing eviction rule already used at save time (oldest-by-`updated`) rather than inventing a second rule; closes F13's R20 gap (201 unpinned entries surviving until a later save) at the moment the cap is actually breached instead of deferring it. |
| D-111 | Ship gates: Gate A after Phase 3 (storage correctness — serialized writer, boundary validation, write ownership/freshness), Gate B after Phase 6 (resume reliability — identity/cancellation, verified outcomes, deferred recovery), Gate C after Phase 8 (product completeness — completion policy, popup reconciliation/accessibility). Phase 9 (regression, docs, store release) follows Gate C and is not itself a gate | APPROVED (Claude) | Claude | All (v4) | Owner-specified gate placement, given a name and rationale here since the prompt states the gates without one. Each gate lands after a coherent, independently-valuable slice (storage safety, then resume correctness, then product features) rather than at arbitrary phase boundaries, so a real-world release decision to ship early has a defensible cut point at each gate. |

---

## Currently blocking

Nothing. D-032/D-033 (privacy policy, store listing) are closed. D-034 (single release vs. split
v1.1 patch) remains open but is optional and non-blocking — v2.0.0 shipped as one release.

Everything else is either approved, done, or Claude Code's to close autonomously.
